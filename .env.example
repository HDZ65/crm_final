# CRM Final - Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git!

# ===========================================
# SECURITY SECRETS (CRITICAL - Generate new values!)
# ===========================================

# Keycloak OAuth Secret
# Generate with: openssl rand -base64 32
KEYCLOAK_SECRET=

# NextAuth Secret for JWT signing
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# Encryption key for sensitive data (emails, tokens)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=

# ===========================================
# DATABASE (Default for development)
# ===========================================
DB_HOST=postgres-main
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres

# ===========================================
# KEYCLOAK CONFIGURATION
# ===========================================
KEYCLOAK_ISSUER=https://finanssorssoapi.com/realms/master
KEYCLOAK_ID=module-rh
KEYCLOAK_REALM=master

# ===========================================
# APPLICATION URLS
# ===========================================
NEXTAUTH_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000

# ===========================================
# ENVIRONMENT
# ===========================================
NODE_ENV=development

# ===========================================
# GENERATION SCRIPT
# ===========================================
# Run this to generate secure secrets:
# 
# echo "KEYCLOAK_SECRET=$(openssl rand -base64 32)" >> .env
# echo "NEXTAUTH_SECRET=$(openssl rand -base64 32)" >> .env
# echo "ENCRYPTION_KEY=$(openssl rand -base64 32)" >> .env
