Task 20 — RG3: Complete downstream handlers for auto-reactivation chain
========================================================================

Date: 2026-03-16

## Files Modified

1. services/service-core/src/infrastructure/messaging/nats/handlers/depanssur/abonnement-restored.handler.ts
   - MODIFIED: Completed stub → full implementation
   - Injects AbonnementService, finds abonnement by ID, validates status is SUSPENDU_IMPAYE, updates to ACTIF
   - Uses AbonnementService.update() which auto-tracks history + publishes status_changed event

2. services/service-telecom/src/infrastructure/messaging/nats/handlers/abonnement-restored.handler.ts
   - CREATED: New handler for telecom line reactivation
   - Subscribes to abonnement.depanssur.restored
   - Uses CarrierSelectorService.selectCarrier() → carrier.activateLine()
   - Generates correlationId for tracing

3. services/service-engagement/src/infrastructure/messaging/nats/handlers/abonnement-restored.handler.ts
   - CREATED: New handler for notification creation
   - Subscribes to abonnement.depanssur.restored
   - Creates INFO notification via NotificationService.create()
   - Metadata includes eventType=ABONNEMENT_RESTORED, abonnementId, restoredAt

## Files Modified (Module Registration)

4. services/service-telecom/src/infrastructure/messaging/nats/handlers/index.ts
   - Added export for AbonnementRestoredHandler

5. services/service-telecom/src/provisioning.module.ts
   - Added AbonnementRestoredHandler to imports and providers

6. services/service-engagement/src/infrastructure/messaging/nats/handlers/index.ts
   - Added export for AbonnementRestoredEngagementHandler

7. services/service-engagement/src/engagement.module.ts
   - Added AbonnementRestoredEngagementHandler to imports and providers

## Build Results

- service-core: BUILD SUCCESS (nest build)
- service-telecom: BUILD SUCCESS (nest build)
- service-engagement: BUILD SUCCESS (nest build)

## LSP Diagnostics

All 3 handler files: 0 errors, 0 warnings

## Event Flow (RG3 Auto-Reactivation Chain)

1. service-finance publishes `abonnement.depanssur.restored` after successful payment
2. service-core handler: Updates abonnement status SUSPENDU_IMPAYE → ACTIF
3. service-telecom handler: Reactivates telecom line via CarrierSelectorService
4. service-engagement handler: Creates restoration notification for client
