import { FacturationParEntity } from '../domain/facturation-par.entity';
import { BaseRepositoryPort } from './repository.port';

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface FacturationParRepositoryPort extends BaseRepositoryPort<FacturationParEntity> {
  // Add custom repository methods here
}
