import { GrilleTarifaireEntity } from '../domain/grille-tarifaire.entity';
import { BaseRepositoryPort } from './repository.port';

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface GrilleTarifaireRepositoryPort extends BaseRepositoryPort<GrilleTarifaireEntity> {
  // Add custom repository methods here
}
