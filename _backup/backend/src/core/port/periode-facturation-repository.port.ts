import { PeriodeFacturationEntity } from '../domain/periode-facturation.entity';
import { BaseRepositoryPort } from './repository.port';

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface PeriodeFacturationRepositoryPort extends BaseRepositoryPort<PeriodeFacturationEntity> {
  // Add custom repository methods here
}
