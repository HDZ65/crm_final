import { RolePartenaireEntity } from '../domain/role-partenaire.entity';
import { BaseRepositoryPort } from './repository.port';

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface RolePartenaireRepositoryPort extends BaseRepositoryPort<RolePartenaireEntity> {
  // Add custom repository methods here
}
