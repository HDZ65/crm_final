import { TypeActiviteEntity } from '../domain/type-activite.entity';
import { BaseRepositoryPort } from './repository.port';

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface TypeActiviteRepositoryPort extends BaseRepositoryPort<TypeActiviteEntity> {
  // Add custom repository methods here
}
