services:
  crm-service-commerciaux:
    build:
      context: ../..
      dockerfile: services/service-commerciaux/Dockerfile
      target: development
    container_name: dev-crm-service-commerciaux
    ports:
      - "50053:50053"
    env_file:
      - ../../services/service-commerciaux/.env.development
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    volumes:
      - ../../services/service-commerciaux/src:/app/services/service-commerciaux/src
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50053, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
