services:
  crm-service-commission:
    build:
      context: ../..
      dockerfile: services/service-commission/Dockerfile
      target: development
    container_name: dev-crm-service-commission
    ports:
      - "50054:50054"
    env_file:
      - ../../services/service-commission/.env.development
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    volumes:
      - ../../services/service-commission/src:/app/services/service-commission/src
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50054, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
