services:
  crm-service-documents:
    build:
      context: ../..
      dockerfile: services/service-documents/Dockerfile
      target: development
    container_name: dev-crm-service-documents
    ports:
      - "50057:50057"
    env_file:
      - ../../services/service-documents/.env.development
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    volumes:
      - ../../services/service-documents/src:/app/services/service-documents/src
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50057, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
