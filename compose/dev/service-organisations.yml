services:
  crm-service-organisations:
    build:
      context: ../..
      dockerfile: services/service-organisations/Dockerfile
      target: development
    container_name: dev-crm-service-organisations
    ports:
      - "50062:50062"
    env_file:
      - ../../services/service-organisations/.env.development
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    volumes:
      - ../../services/service-organisations/src:/app/services/service-organisations/src
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50062, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
