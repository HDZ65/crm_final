services:
  crm-service-referentiel:
    build:
      context: ../..
      dockerfile: services/service-referentiel/Dockerfile
      target: development
    container_name: dev-crm-service-referentiel
    ports:
      - "50065:50065"
    env_file:
      - ../../services/service-referentiel/.env.development
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    volumes:
      - ../../services/service-referentiel/src:/app/services/service-referentiel/src
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50065, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
