services:
  crm-service-users:
    build:
      context: ../..
      dockerfile: services/service-users/Dockerfile
      target: development
    container_name: dev-crm-service-users
    ports:
      - "50067:50067"
    env_file:
      - ../../services/service-users/.env.development
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    volumes:
      - ../../services/service-users/src:/app/services/service-users/src
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50067, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
