services:
  crm-service-factures:
    build:
      context: ../..
      dockerfile: services/service-factures/Dockerfile
      target: production
    container_name: prod-crm-service-factures
    ports:
      - "50059:50059"
    env_file:
      - ../../services/service-factures/.env.production
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50059, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
