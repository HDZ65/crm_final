services:
  crm-service-relance:
    build:
      context: ../..
      dockerfile: services/service-relance/Dockerfile
      target: production
    container_name: prod-crm-service-relance
    ports:
      - "50066:50066"
    env_file:
      - ../../services/service-relance/.env.production
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50066, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
