services:
  crm-service-users:
    build:
      context: ../..
      dockerfile: services/service-users/Dockerfile
      target: production
    container_name: prod-crm-service-users
    ports:
      - "50067:50067"
    env_file:
      - ../../services/service-users/.env.production
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50067, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
