services:
  crm-service-commerciaux:
    build:
      context: ../..
      dockerfile: services/service-commerciaux/Dockerfile
      target: production
    container_name: staging-crm-service-commerciaux
    ports:
      - "51053:50053"
    env_file:
      - ../../services/service-commerciaux/.env.staging
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50053, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
