services:
  crm-service-core:
    build:
      context: ../..
      dockerfile: services/service-core/Dockerfile
      target: production
    container_name: staging-crm-service-core
    ports:
      - "127.0.0.1:3412:3052"
    env_file:
      - ../../services/service-core/.env.staging
    environment:
      - NODE_PATH=/app/services/service-core/node_modules
      - DATABASE_URL=postgresql://postgres:postgres@staging-crm-postgres:5432/staging_core
      - MIGRATIONS_RUN=false
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - crm-staging-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(3052, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

networks:
  crm-staging-network:
