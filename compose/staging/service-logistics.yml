services:
  crm-service-logistics:
    build:
      context: ../..
      dockerfile: services/service-logistics/Dockerfile
      target: production
    container_name: staging-crm-service-logistics
    ports:
      - "51060:50060"
    env_file:
      - ../../services/service-logistics/.env.staging
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50060, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
