services:
  crm-service-organisations:
    build:
      context: ../..
      dockerfile: services/service-organisations/Dockerfile
      target: production
    container_name: staging-crm-service-organisations
    ports:
      - "51062:50062"
    env_file:
      - ../../services/service-organisations/.env.staging
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50062, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
