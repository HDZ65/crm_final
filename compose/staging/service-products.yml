services:
  crm-service-products:
    build:
      context: ../..
      dockerfile: services/service-products/Dockerfile
      target: production
    container_name: staging-crm-service-products
    ports:
      - "51064:50064"
    env_file:
      - ../../services/service-products/.env.staging
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50064, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
