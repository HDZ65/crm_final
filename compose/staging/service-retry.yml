services:
  crm-service-retry:
    build:
      context: ../..
      dockerfile: services/service-retry/Dockerfile
      target: production
    container_name: staging-crm-service-retry
    ports:
      - "51070:50070"
    env_file:
      - ../../services/service-retry/.env.staging
    depends_on:
      postgres-main:
        condition: service_healthy
      nats:
        condition: service_started
    networks:
      - crm-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(50070, 'localhost').on('connect', () => process.exit(0))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
