// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: events/document_events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "events";

export interface DocumentSignedEvent {
  signatureRequestId: string;
  documentId: string;
  contratId: string;
  organisationId: string;
  signerEmail: string;
  signedAt: string;
  timestamp: string;
}

export interface DocumentSignatureFailedEvent {
  signatureRequestId: string;
  contratId: string;
  organisationId: string;
  reason: string;
  status: string;
  timestamp: string;
}

function createBaseDocumentSignedEvent(): DocumentSignedEvent {
  return {
    signatureRequestId: "",
    documentId: "",
    contratId: "",
    organisationId: "",
    signerEmail: "",
    signedAt: "",
    timestamp: "",
  };
}

export const DocumentSignedEvent: MessageFns<DocumentSignedEvent> = {
  encode(message: DocumentSignedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signatureRequestId !== "") {
      writer.uint32(10).string(message.signatureRequestId);
    }
    if (message.documentId !== "") {
      writer.uint32(18).string(message.documentId);
    }
    if (message.contratId !== "") {
      writer.uint32(26).string(message.contratId);
    }
    if (message.organisationId !== "") {
      writer.uint32(34).string(message.organisationId);
    }
    if (message.signerEmail !== "") {
      writer.uint32(42).string(message.signerEmail);
    }
    if (message.signedAt !== "") {
      writer.uint32(50).string(message.signedAt);
    }
    if (message.timestamp !== "") {
      writer.uint32(58).string(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DocumentSignedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDocumentSignedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.signatureRequestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.contratId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signerEmail = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.signedAt = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.timestamp = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DocumentSignedEvent {
    return {
      signatureRequestId: isSet(object.signatureRequestId)
        ? globalThis.String(object.signatureRequestId)
        : isSet(object.signature_request_id)
        ? globalThis.String(object.signature_request_id)
        : "",
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
      contratId: isSet(object.contratId)
        ? globalThis.String(object.contratId)
        : isSet(object.contrat_id)
        ? globalThis.String(object.contrat_id)
        : "",
      organisationId: isSet(object.organisationId)
        ? globalThis.String(object.organisationId)
        : isSet(object.organisation_id)
        ? globalThis.String(object.organisation_id)
        : "",
      signerEmail: isSet(object.signerEmail)
        ? globalThis.String(object.signerEmail)
        : isSet(object.signer_email)
        ? globalThis.String(object.signer_email)
        : "",
      signedAt: isSet(object.signedAt)
        ? globalThis.String(object.signedAt)
        : isSet(object.signed_at)
        ? globalThis.String(object.signed_at)
        : "",
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "",
    };
  },

  toJSON(message: DocumentSignedEvent): unknown {
    const obj: any = {};
    if (message.signatureRequestId !== "") {
      obj.signatureRequestId = message.signatureRequestId;
    }
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    if (message.contratId !== "") {
      obj.contratId = message.contratId;
    }
    if (message.organisationId !== "") {
      obj.organisationId = message.organisationId;
    }
    if (message.signerEmail !== "") {
      obj.signerEmail = message.signerEmail;
    }
    if (message.signedAt !== "") {
      obj.signedAt = message.signedAt;
    }
    if (message.timestamp !== "") {
      obj.timestamp = message.timestamp;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DocumentSignedEvent>, I>>(base?: I): DocumentSignedEvent {
    return DocumentSignedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DocumentSignedEvent>, I>>(object: I): DocumentSignedEvent {
    const message = createBaseDocumentSignedEvent();
    message.signatureRequestId = object.signatureRequestId ?? "";
    message.documentId = object.documentId ?? "";
    message.contratId = object.contratId ?? "";
    message.organisationId = object.organisationId ?? "";
    message.signerEmail = object.signerEmail ?? "";
    message.signedAt = object.signedAt ?? "";
    message.timestamp = object.timestamp ?? "";
    return message;
  },
};

function createBaseDocumentSignatureFailedEvent(): DocumentSignatureFailedEvent {
  return { signatureRequestId: "", contratId: "", organisationId: "", reason: "", status: "", timestamp: "" };
}

export const DocumentSignatureFailedEvent: MessageFns<DocumentSignatureFailedEvent> = {
  encode(message: DocumentSignatureFailedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signatureRequestId !== "") {
      writer.uint32(10).string(message.signatureRequestId);
    }
    if (message.contratId !== "") {
      writer.uint32(18).string(message.contratId);
    }
    if (message.organisationId !== "") {
      writer.uint32(26).string(message.organisationId);
    }
    if (message.reason !== "") {
      writer.uint32(34).string(message.reason);
    }
    if (message.status !== "") {
      writer.uint32(42).string(message.status);
    }
    if (message.timestamp !== "") {
      writer.uint32(50).string(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DocumentSignatureFailedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDocumentSignatureFailedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.signatureRequestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contratId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.timestamp = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DocumentSignatureFailedEvent {
    return {
      signatureRequestId: isSet(object.signatureRequestId)
        ? globalThis.String(object.signatureRequestId)
        : isSet(object.signature_request_id)
        ? globalThis.String(object.signature_request_id)
        : "",
      contratId: isSet(object.contratId)
        ? globalThis.String(object.contratId)
        : isSet(object.contrat_id)
        ? globalThis.String(object.contrat_id)
        : "",
      organisationId: isSet(object.organisationId)
        ? globalThis.String(object.organisationId)
        : isSet(object.organisation_id)
        ? globalThis.String(object.organisation_id)
        : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "",
    };
  },

  toJSON(message: DocumentSignatureFailedEvent): unknown {
    const obj: any = {};
    if (message.signatureRequestId !== "") {
      obj.signatureRequestId = message.signatureRequestId;
    }
    if (message.contratId !== "") {
      obj.contratId = message.contratId;
    }
    if (message.organisationId !== "") {
      obj.organisationId = message.organisationId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.timestamp !== "") {
      obj.timestamp = message.timestamp;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DocumentSignatureFailedEvent>, I>>(base?: I): DocumentSignatureFailedEvent {
    return DocumentSignatureFailedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DocumentSignatureFailedEvent>, I>>(object: I): DocumentSignatureFailedEvent {
    const message = createBaseDocumentSignatureFailedEvent();
    message.signatureRequestId = object.signatureRequestId ?? "";
    message.contratId = object.contratId ?? "";
    message.organisationId = object.organisationId ?? "";
    message.reason = object.reason ?? "";
    message.status = object.status ?? "";
    message.timestamp = object.timestamp ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
