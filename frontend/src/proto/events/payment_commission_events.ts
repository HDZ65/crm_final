// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: events/payment_commission_events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "events";

export interface EcheanceEncaisseeEvent {
  echeanceId: string;
  contratId: string;
  organisationId: string;
  montant: number;
  dateEncaissement: string;
}

function createBaseEcheanceEncaisseeEvent(): EcheanceEncaisseeEvent {
  return { echeanceId: "", contratId: "", organisationId: "", montant: 0, dateEncaissement: "" };
}

export const EcheanceEncaisseeEvent: MessageFns<EcheanceEncaisseeEvent> = {
  encode(message: EcheanceEncaisseeEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.echeanceId !== "") {
      writer.uint32(10).string(message.echeanceId);
    }
    if (message.contratId !== "") {
      writer.uint32(18).string(message.contratId);
    }
    if (message.organisationId !== "") {
      writer.uint32(26).string(message.organisationId);
    }
    if (message.montant !== 0) {
      writer.uint32(33).double(message.montant);
    }
    if (message.dateEncaissement !== "") {
      writer.uint32(42).string(message.dateEncaissement);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EcheanceEncaisseeEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEcheanceEncaisseeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.echeanceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contratId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.montant = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.dateEncaissement = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EcheanceEncaisseeEvent {
    return {
      echeanceId: isSet(object.echeanceId)
        ? globalThis.String(object.echeanceId)
        : isSet(object.echeance_id)
        ? globalThis.String(object.echeance_id)
        : "",
      contratId: isSet(object.contratId)
        ? globalThis.String(object.contratId)
        : isSet(object.contrat_id)
        ? globalThis.String(object.contrat_id)
        : "",
      organisationId: isSet(object.organisationId)
        ? globalThis.String(object.organisationId)
        : isSet(object.organisation_id)
        ? globalThis.String(object.organisation_id)
        : "",
      montant: isSet(object.montant) ? globalThis.Number(object.montant) : 0,
      dateEncaissement: isSet(object.dateEncaissement)
        ? globalThis.String(object.dateEncaissement)
        : isSet(object.date_encaissement)
        ? globalThis.String(object.date_encaissement)
        : "",
    };
  },

  toJSON(message: EcheanceEncaisseeEvent): unknown {
    const obj: any = {};
    if (message.echeanceId !== "") {
      obj.echeanceId = message.echeanceId;
    }
    if (message.contratId !== "") {
      obj.contratId = message.contratId;
    }
    if (message.organisationId !== "") {
      obj.organisationId = message.organisationId;
    }
    if (message.montant !== 0) {
      obj.montant = message.montant;
    }
    if (message.dateEncaissement !== "") {
      obj.dateEncaissement = message.dateEncaissement;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EcheanceEncaisseeEvent>, I>>(base?: I): EcheanceEncaisseeEvent {
    return EcheanceEncaisseeEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EcheanceEncaisseeEvent>, I>>(object: I): EcheanceEncaisseeEvent {
    const message = createBaseEcheanceEncaisseeEvent();
    message.echeanceId = object.echeanceId ?? "";
    message.contratId = object.contratId ?? "";
    message.organisationId = object.organisationId ?? "";
    message.montant = object.montant ?? 0;
    message.dateEncaissement = object.dateEncaissement ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
