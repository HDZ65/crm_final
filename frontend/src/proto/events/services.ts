// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: events/services.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";

export const protobufPackage = "events";

/** Emitted when a new demande de conciergerie is created */
export interface DemandeCreatedEvent {
  /** Event metadata */
  eventId: string;
  /** Unix timestamp in milliseconds */
  timestamp: number;
  /** Optional, for tracing */
  correlationId: string;
  /** Event data */
  demandeId: string;
  /** Organisation ID (tenant) */
  organisationId: string;
  /** Client ID */
  clientId: string;
  /** Demande title */
  titre: string;
  /** Category */
  categorie: string;
  /** Priority */
  priorite: string;
  /** Creation timestamp */
  createdAt: Date | undefined;
}

/** Emitted when a demande de conciergerie is updated */
export interface DemandeUpdatedEvent {
  /** Event metadata */
  eventId: string;
  /** Unix timestamp in milliseconds */
  timestamp: number;
  /** Optional, for tracing */
  correlationId: string;
  /** Event data */
  demandeId: string;
  /** Organisation ID (tenant) */
  organisationId: string;
  /** Previous status */
  ancienStatut: string;
  /** New status */
  nouveauStatut: string;
  /** Assigned agent (if changed) */
  assigneA?:
    | string
    | undefined;
  /** Update timestamp */
  updatedAt: Date | undefined;
}

/** Emitted when a demande de conciergerie is closed */
export interface DemandeClosedEvent {
  /** Event metadata */
  eventId: string;
  /** Unix timestamp in milliseconds */
  timestamp: number;
  /** Optional, for tracing */
  correlationId: string;
  /** Event data */
  demandeId: string;
  /** Organisation ID (tenant) */
  organisationId: string;
  /** Client ID */
  clientId: string;
  /** Resolution summary */
  resolutionNotes: string;
  /** Closure timestamp */
  closedAt: Date | undefined;
}

/** Emitted when cas juridiques are synced from Justi+ partner */
export interface CasJuridiqueSyncedEvent {
  /** Event metadata */
  eventId: string;
  /** Unix timestamp in milliseconds */
  timestamp: number;
  /** Optional, for tracing */
  correlationId: string;
  /** Event data */
  organisationId: string;
  /** Sync batch ID */
  syncId: string;
  /** Number of cases created */
  casCrees: number;
  /** Number of cases updated */
  casMisAJour: number;
  /** Number of errors */
  erreurs: number;
  /** Sync timestamp */
  syncedAt: Date | undefined;
}

/** Emitted when cashback operations are synced from WinCash partner */
export interface CashbackSyncedEvent {
  /** Event metadata */
  eventId: string;
  /** Unix timestamp in milliseconds */
  timestamp: number;
  /** Optional, for tracing */
  correlationId: string;
  /** Event data */
  organisationId: string;
  /** Sync batch ID */
  syncId: string;
  /** Number of operations created */
  operationsCreees: number;
  /** Number of operations updated */
  operationsMisesAJour: number;
  /** Number of errors */
  erreurs: number;
  /** Total amount synced */
  totalMontant: number;
  /** Sync timestamp */
  syncedAt: Date | undefined;
}

function createBaseDemandeCreatedEvent(): DemandeCreatedEvent {
  return {
    eventId: "",
    timestamp: 0,
    correlationId: "",
    demandeId: "",
    organisationId: "",
    clientId: "",
    titre: "",
    categorie: "",
    priorite: "",
    createdAt: undefined,
  };
}

export const DemandeCreatedEvent: MessageFns<DemandeCreatedEvent> = {
  encode(message: DemandeCreatedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).int64(message.timestamp);
    }
    if (message.correlationId !== "") {
      writer.uint32(26).string(message.correlationId);
    }
    if (message.demandeId !== "") {
      writer.uint32(34).string(message.demandeId);
    }
    if (message.organisationId !== "") {
      writer.uint32(42).string(message.organisationId);
    }
    if (message.clientId !== "") {
      writer.uint32(50).string(message.clientId);
    }
    if (message.titre !== "") {
      writer.uint32(58).string(message.titre);
    }
    if (message.categorie !== "") {
      writer.uint32(66).string(message.categorie);
    }
    if (message.priorite !== "") {
      writer.uint32(74).string(message.priorite);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DemandeCreatedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDemandeCreatedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.demandeId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.titre = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.categorie = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.priorite = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DemandeCreatedEvent {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : "",
      demandeId: isSet(object.demandeId)
        ? globalThis.String(object.demandeId)
        : isSet(object.demande_id)
        ? globalThis.String(object.demande_id)
        : "",
      organisationId: isSet(object.organisationId)
        ? globalThis.String(object.organisationId)
        : isSet(object.organisation_id)
        ? globalThis.String(object.organisation_id)
        : "",
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      titre: isSet(object.titre) ? globalThis.String(object.titre) : "",
      categorie: isSet(object.categorie) ? globalThis.String(object.categorie) : "",
      priorite: isSet(object.priorite) ? globalThis.String(object.priorite) : "",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
    };
  },

  toJSON(message: DemandeCreatedEvent): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.correlationId !== "") {
      obj.correlationId = message.correlationId;
    }
    if (message.demandeId !== "") {
      obj.demandeId = message.demandeId;
    }
    if (message.organisationId !== "") {
      obj.organisationId = message.organisationId;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.titre !== "") {
      obj.titre = message.titre;
    }
    if (message.categorie !== "") {
      obj.categorie = message.categorie;
    }
    if (message.priorite !== "") {
      obj.priorite = message.priorite;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DemandeCreatedEvent>, I>>(base?: I): DemandeCreatedEvent {
    return DemandeCreatedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DemandeCreatedEvent>, I>>(object: I): DemandeCreatedEvent {
    const message = createBaseDemandeCreatedEvent();
    message.eventId = object.eventId ?? "";
    message.timestamp = object.timestamp ?? 0;
    message.correlationId = object.correlationId ?? "";
    message.demandeId = object.demandeId ?? "";
    message.organisationId = object.organisationId ?? "";
    message.clientId = object.clientId ?? "";
    message.titre = object.titre ?? "";
    message.categorie = object.categorie ?? "";
    message.priorite = object.priorite ?? "";
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseDemandeUpdatedEvent(): DemandeUpdatedEvent {
  return {
    eventId: "",
    timestamp: 0,
    correlationId: "",
    demandeId: "",
    organisationId: "",
    ancienStatut: "",
    nouveauStatut: "",
    assigneA: undefined,
    updatedAt: undefined,
  };
}

export const DemandeUpdatedEvent: MessageFns<DemandeUpdatedEvent> = {
  encode(message: DemandeUpdatedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).int64(message.timestamp);
    }
    if (message.correlationId !== "") {
      writer.uint32(26).string(message.correlationId);
    }
    if (message.demandeId !== "") {
      writer.uint32(34).string(message.demandeId);
    }
    if (message.organisationId !== "") {
      writer.uint32(42).string(message.organisationId);
    }
    if (message.ancienStatut !== "") {
      writer.uint32(50).string(message.ancienStatut);
    }
    if (message.nouveauStatut !== "") {
      writer.uint32(58).string(message.nouveauStatut);
    }
    if (message.assigneA !== undefined) {
      writer.uint32(66).string(message.assigneA);
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DemandeUpdatedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDemandeUpdatedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.demandeId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.ancienStatut = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.nouveauStatut = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.assigneA = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DemandeUpdatedEvent {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : "",
      demandeId: isSet(object.demandeId)
        ? globalThis.String(object.demandeId)
        : isSet(object.demande_id)
        ? globalThis.String(object.demande_id)
        : "",
      organisationId: isSet(object.organisationId)
        ? globalThis.String(object.organisationId)
        : isSet(object.organisation_id)
        ? globalThis.String(object.organisation_id)
        : "",
      ancienStatut: isSet(object.ancienStatut)
        ? globalThis.String(object.ancienStatut)
        : isSet(object.ancien_statut)
        ? globalThis.String(object.ancien_statut)
        : "",
      nouveauStatut: isSet(object.nouveauStatut)
        ? globalThis.String(object.nouveauStatut)
        : isSet(object.nouveau_statut)
        ? globalThis.String(object.nouveau_statut)
        : "",
      assigneA: isSet(object.assigneA)
        ? globalThis.String(object.assigneA)
        : isSet(object.assigne_a)
        ? globalThis.String(object.assigne_a)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: DemandeUpdatedEvent): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.correlationId !== "") {
      obj.correlationId = message.correlationId;
    }
    if (message.demandeId !== "") {
      obj.demandeId = message.demandeId;
    }
    if (message.organisationId !== "") {
      obj.organisationId = message.organisationId;
    }
    if (message.ancienStatut !== "") {
      obj.ancienStatut = message.ancienStatut;
    }
    if (message.nouveauStatut !== "") {
      obj.nouveauStatut = message.nouveauStatut;
    }
    if (message.assigneA !== undefined) {
      obj.assigneA = message.assigneA;
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DemandeUpdatedEvent>, I>>(base?: I): DemandeUpdatedEvent {
    return DemandeUpdatedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DemandeUpdatedEvent>, I>>(object: I): DemandeUpdatedEvent {
    const message = createBaseDemandeUpdatedEvent();
    message.eventId = object.eventId ?? "";
    message.timestamp = object.timestamp ?? 0;
    message.correlationId = object.correlationId ?? "";
    message.demandeId = object.demandeId ?? "";
    message.organisationId = object.organisationId ?? "";
    message.ancienStatut = object.ancienStatut ?? "";
    message.nouveauStatut = object.nouveauStatut ?? "";
    message.assigneA = object.assigneA ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseDemandeClosedEvent(): DemandeClosedEvent {
  return {
    eventId: "",
    timestamp: 0,
    correlationId: "",
    demandeId: "",
    organisationId: "",
    clientId: "",
    resolutionNotes: "",
    closedAt: undefined,
  };
}

export const DemandeClosedEvent: MessageFns<DemandeClosedEvent> = {
  encode(message: DemandeClosedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).int64(message.timestamp);
    }
    if (message.correlationId !== "") {
      writer.uint32(26).string(message.correlationId);
    }
    if (message.demandeId !== "") {
      writer.uint32(34).string(message.demandeId);
    }
    if (message.organisationId !== "") {
      writer.uint32(42).string(message.organisationId);
    }
    if (message.clientId !== "") {
      writer.uint32(50).string(message.clientId);
    }
    if (message.resolutionNotes !== "") {
      writer.uint32(58).string(message.resolutionNotes);
    }
    if (message.closedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.closedAt), writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DemandeClosedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDemandeClosedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.demandeId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.resolutionNotes = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.closedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DemandeClosedEvent {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : "",
      demandeId: isSet(object.demandeId)
        ? globalThis.String(object.demandeId)
        : isSet(object.demande_id)
        ? globalThis.String(object.demande_id)
        : "",
      organisationId: isSet(object.organisationId)
        ? globalThis.String(object.organisationId)
        : isSet(object.organisation_id)
        ? globalThis.String(object.organisation_id)
        : "",
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      resolutionNotes: isSet(object.resolutionNotes)
        ? globalThis.String(object.resolutionNotes)
        : isSet(object.resolution_notes)
        ? globalThis.String(object.resolution_notes)
        : "",
      closedAt: isSet(object.closedAt)
        ? fromJsonTimestamp(object.closedAt)
        : isSet(object.closed_at)
        ? fromJsonTimestamp(object.closed_at)
        : undefined,
    };
  },

  toJSON(message: DemandeClosedEvent): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.correlationId !== "") {
      obj.correlationId = message.correlationId;
    }
    if (message.demandeId !== "") {
      obj.demandeId = message.demandeId;
    }
    if (message.organisationId !== "") {
      obj.organisationId = message.organisationId;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.resolutionNotes !== "") {
      obj.resolutionNotes = message.resolutionNotes;
    }
    if (message.closedAt !== undefined) {
      obj.closedAt = message.closedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DemandeClosedEvent>, I>>(base?: I): DemandeClosedEvent {
    return DemandeClosedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DemandeClosedEvent>, I>>(object: I): DemandeClosedEvent {
    const message = createBaseDemandeClosedEvent();
    message.eventId = object.eventId ?? "";
    message.timestamp = object.timestamp ?? 0;
    message.correlationId = object.correlationId ?? "";
    message.demandeId = object.demandeId ?? "";
    message.organisationId = object.organisationId ?? "";
    message.clientId = object.clientId ?? "";
    message.resolutionNotes = object.resolutionNotes ?? "";
    message.closedAt = object.closedAt ?? undefined;
    return message;
  },
};

function createBaseCasJuridiqueSyncedEvent(): CasJuridiqueSyncedEvent {
  return {
    eventId: "",
    timestamp: 0,
    correlationId: "",
    organisationId: "",
    syncId: "",
    casCrees: 0,
    casMisAJour: 0,
    erreurs: 0,
    syncedAt: undefined,
  };
}

export const CasJuridiqueSyncedEvent: MessageFns<CasJuridiqueSyncedEvent> = {
  encode(message: CasJuridiqueSyncedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).int64(message.timestamp);
    }
    if (message.correlationId !== "") {
      writer.uint32(26).string(message.correlationId);
    }
    if (message.organisationId !== "") {
      writer.uint32(34).string(message.organisationId);
    }
    if (message.syncId !== "") {
      writer.uint32(42).string(message.syncId);
    }
    if (message.casCrees !== 0) {
      writer.uint32(48).int32(message.casCrees);
    }
    if (message.casMisAJour !== 0) {
      writer.uint32(56).int32(message.casMisAJour);
    }
    if (message.erreurs !== 0) {
      writer.uint32(64).int32(message.erreurs);
    }
    if (message.syncedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.syncedAt), writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CasJuridiqueSyncedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCasJuridiqueSyncedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.syncId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.casCrees = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.casMisAJour = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.erreurs = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.syncedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CasJuridiqueSyncedEvent {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : "",
      organisationId: isSet(object.organisationId)
        ? globalThis.String(object.organisationId)
        : isSet(object.organisation_id)
        ? globalThis.String(object.organisation_id)
        : "",
      syncId: isSet(object.syncId)
        ? globalThis.String(object.syncId)
        : isSet(object.sync_id)
        ? globalThis.String(object.sync_id)
        : "",
      casCrees: isSet(object.casCrees)
        ? globalThis.Number(object.casCrees)
        : isSet(object.cas_crees)
        ? globalThis.Number(object.cas_crees)
        : 0,
      casMisAJour: isSet(object.casMisAJour)
        ? globalThis.Number(object.casMisAJour)
        : isSet(object.cas_mis_a_jour)
        ? globalThis.Number(object.cas_mis_a_jour)
        : 0,
      erreurs: isSet(object.erreurs) ? globalThis.Number(object.erreurs) : 0,
      syncedAt: isSet(object.syncedAt)
        ? fromJsonTimestamp(object.syncedAt)
        : isSet(object.synced_at)
        ? fromJsonTimestamp(object.synced_at)
        : undefined,
    };
  },

  toJSON(message: CasJuridiqueSyncedEvent): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.correlationId !== "") {
      obj.correlationId = message.correlationId;
    }
    if (message.organisationId !== "") {
      obj.organisationId = message.organisationId;
    }
    if (message.syncId !== "") {
      obj.syncId = message.syncId;
    }
    if (message.casCrees !== 0) {
      obj.casCrees = Math.round(message.casCrees);
    }
    if (message.casMisAJour !== 0) {
      obj.casMisAJour = Math.round(message.casMisAJour);
    }
    if (message.erreurs !== 0) {
      obj.erreurs = Math.round(message.erreurs);
    }
    if (message.syncedAt !== undefined) {
      obj.syncedAt = message.syncedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CasJuridiqueSyncedEvent>, I>>(base?: I): CasJuridiqueSyncedEvent {
    return CasJuridiqueSyncedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CasJuridiqueSyncedEvent>, I>>(object: I): CasJuridiqueSyncedEvent {
    const message = createBaseCasJuridiqueSyncedEvent();
    message.eventId = object.eventId ?? "";
    message.timestamp = object.timestamp ?? 0;
    message.correlationId = object.correlationId ?? "";
    message.organisationId = object.organisationId ?? "";
    message.syncId = object.syncId ?? "";
    message.casCrees = object.casCrees ?? 0;
    message.casMisAJour = object.casMisAJour ?? 0;
    message.erreurs = object.erreurs ?? 0;
    message.syncedAt = object.syncedAt ?? undefined;
    return message;
  },
};

function createBaseCashbackSyncedEvent(): CashbackSyncedEvent {
  return {
    eventId: "",
    timestamp: 0,
    correlationId: "",
    organisationId: "",
    syncId: "",
    operationsCreees: 0,
    operationsMisesAJour: 0,
    erreurs: 0,
    totalMontant: 0,
    syncedAt: undefined,
  };
}

export const CashbackSyncedEvent: MessageFns<CashbackSyncedEvent> = {
  encode(message: CashbackSyncedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).int64(message.timestamp);
    }
    if (message.correlationId !== "") {
      writer.uint32(26).string(message.correlationId);
    }
    if (message.organisationId !== "") {
      writer.uint32(34).string(message.organisationId);
    }
    if (message.syncId !== "") {
      writer.uint32(42).string(message.syncId);
    }
    if (message.operationsCreees !== 0) {
      writer.uint32(48).int32(message.operationsCreees);
    }
    if (message.operationsMisesAJour !== 0) {
      writer.uint32(56).int32(message.operationsMisesAJour);
    }
    if (message.erreurs !== 0) {
      writer.uint32(64).int32(message.erreurs);
    }
    if (message.totalMontant !== 0) {
      writer.uint32(73).double(message.totalMontant);
    }
    if (message.syncedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.syncedAt), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CashbackSyncedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCashbackSyncedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.syncId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.operationsCreees = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.operationsMisesAJour = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.erreurs = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 73) {
            break;
          }

          message.totalMontant = reader.double();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.syncedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CashbackSyncedEvent {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : "",
      organisationId: isSet(object.organisationId)
        ? globalThis.String(object.organisationId)
        : isSet(object.organisation_id)
        ? globalThis.String(object.organisation_id)
        : "",
      syncId: isSet(object.syncId)
        ? globalThis.String(object.syncId)
        : isSet(object.sync_id)
        ? globalThis.String(object.sync_id)
        : "",
      operationsCreees: isSet(object.operationsCreees)
        ? globalThis.Number(object.operationsCreees)
        : isSet(object.operations_creees)
        ? globalThis.Number(object.operations_creees)
        : 0,
      operationsMisesAJour: isSet(object.operationsMisesAJour)
        ? globalThis.Number(object.operationsMisesAJour)
        : isSet(object.operations_mises_a_jour)
        ? globalThis.Number(object.operations_mises_a_jour)
        : 0,
      erreurs: isSet(object.erreurs) ? globalThis.Number(object.erreurs) : 0,
      totalMontant: isSet(object.totalMontant)
        ? globalThis.Number(object.totalMontant)
        : isSet(object.total_montant)
        ? globalThis.Number(object.total_montant)
        : 0,
      syncedAt: isSet(object.syncedAt)
        ? fromJsonTimestamp(object.syncedAt)
        : isSet(object.synced_at)
        ? fromJsonTimestamp(object.synced_at)
        : undefined,
    };
  },

  toJSON(message: CashbackSyncedEvent): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.correlationId !== "") {
      obj.correlationId = message.correlationId;
    }
    if (message.organisationId !== "") {
      obj.organisationId = message.organisationId;
    }
    if (message.syncId !== "") {
      obj.syncId = message.syncId;
    }
    if (message.operationsCreees !== 0) {
      obj.operationsCreees = Math.round(message.operationsCreees);
    }
    if (message.operationsMisesAJour !== 0) {
      obj.operationsMisesAJour = Math.round(message.operationsMisesAJour);
    }
    if (message.erreurs !== 0) {
      obj.erreurs = Math.round(message.erreurs);
    }
    if (message.totalMontant !== 0) {
      obj.totalMontant = message.totalMontant;
    }
    if (message.syncedAt !== undefined) {
      obj.syncedAt = message.syncedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CashbackSyncedEvent>, I>>(base?: I): CashbackSyncedEvent {
    return CashbackSyncedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CashbackSyncedEvent>, I>>(object: I): CashbackSyncedEvent {
    const message = createBaseCashbackSyncedEvent();
    message.eventId = object.eventId ?? "";
    message.timestamp = object.timestamp ?? 0;
    message.correlationId = object.correlationId ?? "";
    message.organisationId = object.organisationId ?? "";
    message.syncId = object.syncId ?? "";
    message.operationsCreees = object.operationsCreees ?? 0;
    message.operationsMisesAJour = object.operationsMisesAJour ?? 0;
    message.erreurs = object.erreurs ?? 0;
    message.totalMontant = object.totalMontant ?? 0;
    message.syncedAt = object.syncedAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
