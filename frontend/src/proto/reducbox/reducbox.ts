// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: reducbox/reducbox.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, UntypedServiceImplementation } from "@grpc/grpc-js";

export const protobufPackage = "reducbox";

export enum ReducBoxAccessStatus {
  REDUC_BOX_ACCESS_STATUS_UNSPECIFIED = 0,
  REDUC_BOX_ACCESS_STATUS_PENDING = 1,
  REDUC_BOX_ACCESS_STATUS_ACTIVE = 2,
  REDUC_BOX_ACCESS_STATUS_SUSPENDED = 3,
  REDUC_BOX_ACCESS_STATUS_CANCELLED = 4,
  UNRECOGNIZED = -1,
}

export function reducBoxAccessStatusFromJSON(object: any): ReducBoxAccessStatus {
  switch (object) {
    case 0:
    case "REDUC_BOX_ACCESS_STATUS_UNSPECIFIED":
      return ReducBoxAccessStatus.REDUC_BOX_ACCESS_STATUS_UNSPECIFIED;
    case 1:
    case "REDUC_BOX_ACCESS_STATUS_PENDING":
      return ReducBoxAccessStatus.REDUC_BOX_ACCESS_STATUS_PENDING;
    case 2:
    case "REDUC_BOX_ACCESS_STATUS_ACTIVE":
      return ReducBoxAccessStatus.REDUC_BOX_ACCESS_STATUS_ACTIVE;
    case 3:
    case "REDUC_BOX_ACCESS_STATUS_SUSPENDED":
      return ReducBoxAccessStatus.REDUC_BOX_ACCESS_STATUS_SUSPENDED;
    case 4:
    case "REDUC_BOX_ACCESS_STATUS_CANCELLED":
      return ReducBoxAccessStatus.REDUC_BOX_ACCESS_STATUS_CANCELLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ReducBoxAccessStatus.UNRECOGNIZED;
  }
}

export function reducBoxAccessStatusToJSON(object: ReducBoxAccessStatus): string {
  switch (object) {
    case ReducBoxAccessStatus.REDUC_BOX_ACCESS_STATUS_UNSPECIFIED:
      return "REDUC_BOX_ACCESS_STATUS_UNSPECIFIED";
    case ReducBoxAccessStatus.REDUC_BOX_ACCESS_STATUS_PENDING:
      return "REDUC_BOX_ACCESS_STATUS_PENDING";
    case ReducBoxAccessStatus.REDUC_BOX_ACCESS_STATUS_ACTIVE:
      return "REDUC_BOX_ACCESS_STATUS_ACTIVE";
    case ReducBoxAccessStatus.REDUC_BOX_ACCESS_STATUS_SUSPENDED:
      return "REDUC_BOX_ACCESS_STATUS_SUSPENDED";
    case ReducBoxAccessStatus.REDUC_BOX_ACCESS_STATUS_CANCELLED:
      return "REDUC_BOX_ACCESS_STATUS_CANCELLED";
    case ReducBoxAccessStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ReducBoxAccess {
  id: string;
  clientId: string;
  contratId: string;
  status: ReducBoxAccessStatus;
  createdAt: string;
  suspendedAt?: string | undefined;
  restoredAt?: string | undefined;
}

export interface CreateAccessRequest {
  clientId: string;
  contratId: string;
}

export interface CreateAccessResponse {
  access: ReducBoxAccess | undefined;
}

export interface SuspendAccessRequest {
  id: string;
}

export interface SuspendAccessResponse {
  access: ReducBoxAccess | undefined;
}

export interface RestoreAccessRequest {
  id: string;
}

export interface RestoreAccessResponse {
  access: ReducBoxAccess | undefined;
}

export interface GetAccessStatusRequest {
  id: string;
}

export interface GetAccessStatusResponse {
  access: ReducBoxAccess | undefined;
}

export interface ListAccessByClientRequest {
  clientId: string;
  pagination: Pagination | undefined;
}

export interface ListAccessByClientResponse {
  accesses: ReducBoxAccess[];
  pagination: PaginationResult | undefined;
}

export interface ReducBoxAccessCreatedEvent {
  id: string;
  clientId: string;
  contratId: string;
  createdAt: string;
}

export interface ReducBoxAccessSuspendedEvent {
  id: string;
  clientId: string;
  suspendedAt: string;
}

export interface ReducBoxAccessRestoredEvent {
  id: string;
  clientId: string;
  restoredAt: string;
}

export interface Pagination {
  page: number;
  limit: number;
  sortBy: string;
  sortOrder: string;
}

export interface PaginationResult {
  total: number;
  page: number;
  limit: number;
  totalPages: number;
}

function createBaseReducBoxAccess(): ReducBoxAccess {
  return {
    id: "",
    clientId: "",
    contratId: "",
    status: 0,
    createdAt: "",
    suspendedAt: undefined,
    restoredAt: undefined,
  };
}

export const ReducBoxAccess: MessageFns<ReducBoxAccess> = {
  encode(message: ReducBoxAccess, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.clientId !== "") {
      writer.uint32(18).string(message.clientId);
    }
    if (message.contratId !== "") {
      writer.uint32(26).string(message.contratId);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.createdAt !== "") {
      writer.uint32(42).string(message.createdAt);
    }
    if (message.suspendedAt !== undefined) {
      writer.uint32(50).string(message.suspendedAt);
    }
    if (message.restoredAt !== undefined) {
      writer.uint32(58).string(message.restoredAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReducBoxAccess {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReducBoxAccess();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.contratId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.suspendedAt = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.restoredAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReducBoxAccess {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      contratId: isSet(object.contratId)
        ? globalThis.String(object.contratId)
        : isSet(object.contrat_id)
        ? globalThis.String(object.contrat_id)
        : "",
      status: isSet(object.status) ? reducBoxAccessStatusFromJSON(object.status) : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      suspendedAt: isSet(object.suspendedAt)
        ? globalThis.String(object.suspendedAt)
        : isSet(object.suspended_at)
        ? globalThis.String(object.suspended_at)
        : undefined,
      restoredAt: isSet(object.restoredAt)
        ? globalThis.String(object.restoredAt)
        : isSet(object.restored_at)
        ? globalThis.String(object.restored_at)
        : undefined,
    };
  },

  toJSON(message: ReducBoxAccess): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.contratId !== "") {
      obj.contratId = message.contratId;
    }
    if (message.status !== 0) {
      obj.status = reducBoxAccessStatusToJSON(message.status);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.suspendedAt !== undefined) {
      obj.suspendedAt = message.suspendedAt;
    }
    if (message.restoredAt !== undefined) {
      obj.restoredAt = message.restoredAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReducBoxAccess>, I>>(base?: I): ReducBoxAccess {
    return ReducBoxAccess.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReducBoxAccess>, I>>(object: I): ReducBoxAccess {
    const message = createBaseReducBoxAccess();
    message.id = object.id ?? "";
    message.clientId = object.clientId ?? "";
    message.contratId = object.contratId ?? "";
    message.status = object.status ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.suspendedAt = object.suspendedAt ?? undefined;
    message.restoredAt = object.restoredAt ?? undefined;
    return message;
  },
};

function createBaseCreateAccessRequest(): CreateAccessRequest {
  return { clientId: "", contratId: "" };
}

export const CreateAccessRequest: MessageFns<CreateAccessRequest> = {
  encode(message: CreateAccessRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientId !== "") {
      writer.uint32(10).string(message.clientId);
    }
    if (message.contratId !== "") {
      writer.uint32(18).string(message.contratId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateAccessRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateAccessRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contratId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateAccessRequest {
    return {
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      contratId: isSet(object.contratId)
        ? globalThis.String(object.contratId)
        : isSet(object.contrat_id)
        ? globalThis.String(object.contrat_id)
        : "",
    };
  },

  toJSON(message: CreateAccessRequest): unknown {
    const obj: any = {};
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.contratId !== "") {
      obj.contratId = message.contratId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateAccessRequest>, I>>(base?: I): CreateAccessRequest {
    return CreateAccessRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateAccessRequest>, I>>(object: I): CreateAccessRequest {
    const message = createBaseCreateAccessRequest();
    message.clientId = object.clientId ?? "";
    message.contratId = object.contratId ?? "";
    return message;
  },
};

function createBaseCreateAccessResponse(): CreateAccessResponse {
  return { access: undefined };
}

export const CreateAccessResponse: MessageFns<CreateAccessResponse> = {
  encode(message: CreateAccessResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.access !== undefined) {
      ReducBoxAccess.encode(message.access, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateAccessResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateAccessResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.access = ReducBoxAccess.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateAccessResponse {
    return { access: isSet(object.access) ? ReducBoxAccess.fromJSON(object.access) : undefined };
  },

  toJSON(message: CreateAccessResponse): unknown {
    const obj: any = {};
    if (message.access !== undefined) {
      obj.access = ReducBoxAccess.toJSON(message.access);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateAccessResponse>, I>>(base?: I): CreateAccessResponse {
    return CreateAccessResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateAccessResponse>, I>>(object: I): CreateAccessResponse {
    const message = createBaseCreateAccessResponse();
    message.access = (object.access !== undefined && object.access !== null)
      ? ReducBoxAccess.fromPartial(object.access)
      : undefined;
    return message;
  },
};

function createBaseSuspendAccessRequest(): SuspendAccessRequest {
  return { id: "" };
}

export const SuspendAccessRequest: MessageFns<SuspendAccessRequest> = {
  encode(message: SuspendAccessRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SuspendAccessRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSuspendAccessRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SuspendAccessRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: SuspendAccessRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SuspendAccessRequest>, I>>(base?: I): SuspendAccessRequest {
    return SuspendAccessRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SuspendAccessRequest>, I>>(object: I): SuspendAccessRequest {
    const message = createBaseSuspendAccessRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseSuspendAccessResponse(): SuspendAccessResponse {
  return { access: undefined };
}

export const SuspendAccessResponse: MessageFns<SuspendAccessResponse> = {
  encode(message: SuspendAccessResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.access !== undefined) {
      ReducBoxAccess.encode(message.access, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SuspendAccessResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSuspendAccessResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.access = ReducBoxAccess.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SuspendAccessResponse {
    return { access: isSet(object.access) ? ReducBoxAccess.fromJSON(object.access) : undefined };
  },

  toJSON(message: SuspendAccessResponse): unknown {
    const obj: any = {};
    if (message.access !== undefined) {
      obj.access = ReducBoxAccess.toJSON(message.access);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SuspendAccessResponse>, I>>(base?: I): SuspendAccessResponse {
    return SuspendAccessResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SuspendAccessResponse>, I>>(object: I): SuspendAccessResponse {
    const message = createBaseSuspendAccessResponse();
    message.access = (object.access !== undefined && object.access !== null)
      ? ReducBoxAccess.fromPartial(object.access)
      : undefined;
    return message;
  },
};

function createBaseRestoreAccessRequest(): RestoreAccessRequest {
  return { id: "" };
}

export const RestoreAccessRequest: MessageFns<RestoreAccessRequest> = {
  encode(message: RestoreAccessRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RestoreAccessRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRestoreAccessRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RestoreAccessRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: RestoreAccessRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RestoreAccessRequest>, I>>(base?: I): RestoreAccessRequest {
    return RestoreAccessRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RestoreAccessRequest>, I>>(object: I): RestoreAccessRequest {
    const message = createBaseRestoreAccessRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseRestoreAccessResponse(): RestoreAccessResponse {
  return { access: undefined };
}

export const RestoreAccessResponse: MessageFns<RestoreAccessResponse> = {
  encode(message: RestoreAccessResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.access !== undefined) {
      ReducBoxAccess.encode(message.access, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RestoreAccessResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRestoreAccessResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.access = ReducBoxAccess.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RestoreAccessResponse {
    return { access: isSet(object.access) ? ReducBoxAccess.fromJSON(object.access) : undefined };
  },

  toJSON(message: RestoreAccessResponse): unknown {
    const obj: any = {};
    if (message.access !== undefined) {
      obj.access = ReducBoxAccess.toJSON(message.access);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RestoreAccessResponse>, I>>(base?: I): RestoreAccessResponse {
    return RestoreAccessResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RestoreAccessResponse>, I>>(object: I): RestoreAccessResponse {
    const message = createBaseRestoreAccessResponse();
    message.access = (object.access !== undefined && object.access !== null)
      ? ReducBoxAccess.fromPartial(object.access)
      : undefined;
    return message;
  },
};

function createBaseGetAccessStatusRequest(): GetAccessStatusRequest {
  return { id: "" };
}

export const GetAccessStatusRequest: MessageFns<GetAccessStatusRequest> = {
  encode(message: GetAccessStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAccessStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAccessStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAccessStatusRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: GetAccessStatusRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAccessStatusRequest>, I>>(base?: I): GetAccessStatusRequest {
    return GetAccessStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAccessStatusRequest>, I>>(object: I): GetAccessStatusRequest {
    const message = createBaseGetAccessStatusRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseGetAccessStatusResponse(): GetAccessStatusResponse {
  return { access: undefined };
}

export const GetAccessStatusResponse: MessageFns<GetAccessStatusResponse> = {
  encode(message: GetAccessStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.access !== undefined) {
      ReducBoxAccess.encode(message.access, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAccessStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAccessStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.access = ReducBoxAccess.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAccessStatusResponse {
    return { access: isSet(object.access) ? ReducBoxAccess.fromJSON(object.access) : undefined };
  },

  toJSON(message: GetAccessStatusResponse): unknown {
    const obj: any = {};
    if (message.access !== undefined) {
      obj.access = ReducBoxAccess.toJSON(message.access);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAccessStatusResponse>, I>>(base?: I): GetAccessStatusResponse {
    return GetAccessStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAccessStatusResponse>, I>>(object: I): GetAccessStatusResponse {
    const message = createBaseGetAccessStatusResponse();
    message.access = (object.access !== undefined && object.access !== null)
      ? ReducBoxAccess.fromPartial(object.access)
      : undefined;
    return message;
  },
};

function createBaseListAccessByClientRequest(): ListAccessByClientRequest {
  return { clientId: "", pagination: undefined };
}

export const ListAccessByClientRequest: MessageFns<ListAccessByClientRequest> = {
  encode(message: ListAccessByClientRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientId !== "") {
      writer.uint32(10).string(message.clientId);
    }
    if (message.pagination !== undefined) {
      Pagination.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAccessByClientRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAccessByClientRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = Pagination.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAccessByClientRequest {
    return {
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      pagination: isSet(object.pagination) ? Pagination.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: ListAccessByClientRequest): unknown {
    const obj: any = {};
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.pagination !== undefined) {
      obj.pagination = Pagination.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAccessByClientRequest>, I>>(base?: I): ListAccessByClientRequest {
    return ListAccessByClientRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAccessByClientRequest>, I>>(object: I): ListAccessByClientRequest {
    const message = createBaseListAccessByClientRequest();
    message.clientId = object.clientId ?? "";
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? Pagination.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseListAccessByClientResponse(): ListAccessByClientResponse {
  return { accesses: [], pagination: undefined };
}

export const ListAccessByClientResponse: MessageFns<ListAccessByClientResponse> = {
  encode(message: ListAccessByClientResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.accesses) {
      ReducBoxAccess.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PaginationResult.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAccessByClientResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAccessByClientResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accesses.push(ReducBoxAccess.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PaginationResult.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAccessByClientResponse {
    return {
      accesses: globalThis.Array.isArray(object?.accesses)
        ? object.accesses.map((e: any) => ReducBoxAccess.fromJSON(e))
        : [],
      pagination: isSet(object.pagination) ? PaginationResult.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: ListAccessByClientResponse): unknown {
    const obj: any = {};
    if (message.accesses?.length) {
      obj.accesses = message.accesses.map((e) => ReducBoxAccess.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PaginationResult.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAccessByClientResponse>, I>>(base?: I): ListAccessByClientResponse {
    return ListAccessByClientResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAccessByClientResponse>, I>>(object: I): ListAccessByClientResponse {
    const message = createBaseListAccessByClientResponse();
    message.accesses = object.accesses?.map((e) => ReducBoxAccess.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PaginationResult.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseReducBoxAccessCreatedEvent(): ReducBoxAccessCreatedEvent {
  return { id: "", clientId: "", contratId: "", createdAt: "" };
}

export const ReducBoxAccessCreatedEvent: MessageFns<ReducBoxAccessCreatedEvent> = {
  encode(message: ReducBoxAccessCreatedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.clientId !== "") {
      writer.uint32(18).string(message.clientId);
    }
    if (message.contratId !== "") {
      writer.uint32(26).string(message.contratId);
    }
    if (message.createdAt !== "") {
      writer.uint32(34).string(message.createdAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReducBoxAccessCreatedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReducBoxAccessCreatedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.contratId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReducBoxAccessCreatedEvent {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      contratId: isSet(object.contratId)
        ? globalThis.String(object.contratId)
        : isSet(object.contrat_id)
        ? globalThis.String(object.contrat_id)
        : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
    };
  },

  toJSON(message: ReducBoxAccessCreatedEvent): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.contratId !== "") {
      obj.contratId = message.contratId;
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReducBoxAccessCreatedEvent>, I>>(base?: I): ReducBoxAccessCreatedEvent {
    return ReducBoxAccessCreatedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReducBoxAccessCreatedEvent>, I>>(object: I): ReducBoxAccessCreatedEvent {
    const message = createBaseReducBoxAccessCreatedEvent();
    message.id = object.id ?? "";
    message.clientId = object.clientId ?? "";
    message.contratId = object.contratId ?? "";
    message.createdAt = object.createdAt ?? "";
    return message;
  },
};

function createBaseReducBoxAccessSuspendedEvent(): ReducBoxAccessSuspendedEvent {
  return { id: "", clientId: "", suspendedAt: "" };
}

export const ReducBoxAccessSuspendedEvent: MessageFns<ReducBoxAccessSuspendedEvent> = {
  encode(message: ReducBoxAccessSuspendedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.clientId !== "") {
      writer.uint32(18).string(message.clientId);
    }
    if (message.suspendedAt !== "") {
      writer.uint32(26).string(message.suspendedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReducBoxAccessSuspendedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReducBoxAccessSuspendedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.suspendedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReducBoxAccessSuspendedEvent {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      suspendedAt: isSet(object.suspendedAt)
        ? globalThis.String(object.suspendedAt)
        : isSet(object.suspended_at)
        ? globalThis.String(object.suspended_at)
        : "",
    };
  },

  toJSON(message: ReducBoxAccessSuspendedEvent): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.suspendedAt !== "") {
      obj.suspendedAt = message.suspendedAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReducBoxAccessSuspendedEvent>, I>>(base?: I): ReducBoxAccessSuspendedEvent {
    return ReducBoxAccessSuspendedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReducBoxAccessSuspendedEvent>, I>>(object: I): ReducBoxAccessSuspendedEvent {
    const message = createBaseReducBoxAccessSuspendedEvent();
    message.id = object.id ?? "";
    message.clientId = object.clientId ?? "";
    message.suspendedAt = object.suspendedAt ?? "";
    return message;
  },
};

function createBaseReducBoxAccessRestoredEvent(): ReducBoxAccessRestoredEvent {
  return { id: "", clientId: "", restoredAt: "" };
}

export const ReducBoxAccessRestoredEvent: MessageFns<ReducBoxAccessRestoredEvent> = {
  encode(message: ReducBoxAccessRestoredEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.clientId !== "") {
      writer.uint32(18).string(message.clientId);
    }
    if (message.restoredAt !== "") {
      writer.uint32(26).string(message.restoredAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReducBoxAccessRestoredEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReducBoxAccessRestoredEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.restoredAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReducBoxAccessRestoredEvent {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      restoredAt: isSet(object.restoredAt)
        ? globalThis.String(object.restoredAt)
        : isSet(object.restored_at)
        ? globalThis.String(object.restored_at)
        : "",
    };
  },

  toJSON(message: ReducBoxAccessRestoredEvent): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.restoredAt !== "") {
      obj.restoredAt = message.restoredAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReducBoxAccessRestoredEvent>, I>>(base?: I): ReducBoxAccessRestoredEvent {
    return ReducBoxAccessRestoredEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReducBoxAccessRestoredEvent>, I>>(object: I): ReducBoxAccessRestoredEvent {
    const message = createBaseReducBoxAccessRestoredEvent();
    message.id = object.id ?? "";
    message.clientId = object.clientId ?? "";
    message.restoredAt = object.restoredAt ?? "";
    return message;
  },
};

function createBasePagination(): Pagination {
  return { page: 0, limit: 0, sortBy: "", sortOrder: "" };
}

export const Pagination: MessageFns<Pagination> = {
  encode(message: Pagination, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== 0) {
      writer.uint32(8).int32(message.page);
    }
    if (message.limit !== 0) {
      writer.uint32(16).int32(message.limit);
    }
    if (message.sortBy !== "") {
      writer.uint32(26).string(message.sortBy);
    }
    if (message.sortOrder !== "") {
      writer.uint32(34).string(message.sortOrder);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Pagination {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePagination();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sortBy = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sortOrder = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Pagination {
    return {
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      sortBy: isSet(object.sortBy)
        ? globalThis.String(object.sortBy)
        : isSet(object.sort_by)
        ? globalThis.String(object.sort_by)
        : "",
      sortOrder: isSet(object.sortOrder)
        ? globalThis.String(object.sortOrder)
        : isSet(object.sort_order)
        ? globalThis.String(object.sort_order)
        : "",
    };
  },

  toJSON(message: Pagination): unknown {
    const obj: any = {};
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.sortBy !== "") {
      obj.sortBy = message.sortBy;
    }
    if (message.sortOrder !== "") {
      obj.sortOrder = message.sortOrder;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Pagination>, I>>(base?: I): Pagination {
    return Pagination.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Pagination>, I>>(object: I): Pagination {
    const message = createBasePagination();
    message.page = object.page ?? 0;
    message.limit = object.limit ?? 0;
    message.sortBy = object.sortBy ?? "";
    message.sortOrder = object.sortOrder ?? "";
    return message;
  },
};

function createBasePaginationResult(): PaginationResult {
  return { total: 0, page: 0, limit: 0, totalPages: 0 };
}

export const PaginationResult: MessageFns<PaginationResult> = {
  encode(message: PaginationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.total !== 0) {
      writer.uint32(8).int32(message.total);
    }
    if (message.page !== 0) {
      writer.uint32(16).int32(message.page);
    }
    if (message.limit !== 0) {
      writer.uint32(24).int32(message.limit);
    }
    if (message.totalPages !== 0) {
      writer.uint32(32).int32(message.totalPages);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PaginationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePaginationResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalPages = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PaginationResult {
    return {
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      totalPages: isSet(object.totalPages)
        ? globalThis.Number(object.totalPages)
        : isSet(object.total_pages)
        ? globalThis.Number(object.total_pages)
        : 0,
    };
  },

  toJSON(message: PaginationResult): unknown {
    const obj: any = {};
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.totalPages !== 0) {
      obj.totalPages = Math.round(message.totalPages);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PaginationResult>, I>>(base?: I): PaginationResult {
    return PaginationResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PaginationResult>, I>>(object: I): PaginationResult {
    const message = createBasePaginationResult();
    message.total = object.total ?? 0;
    message.page = object.page ?? 0;
    message.limit = object.limit ?? 0;
    message.totalPages = object.totalPages ?? 0;
    return message;
  },
};

export type ReducBoxServiceService = typeof ReducBoxServiceService;
export const ReducBoxServiceService = {
  createAccess: {
    path: "/reducbox.ReducBoxService/CreateAccess",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: CreateAccessRequest): Buffer => Buffer.from(CreateAccessRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateAccessRequest => CreateAccessRequest.decode(value),
    responseSerialize: (value: CreateAccessResponse): Buffer =>
      Buffer.from(CreateAccessResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateAccessResponse => CreateAccessResponse.decode(value),
  },
  suspendAccess: {
    path: "/reducbox.ReducBoxService/SuspendAccess",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: SuspendAccessRequest): Buffer => Buffer.from(SuspendAccessRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SuspendAccessRequest => SuspendAccessRequest.decode(value),
    responseSerialize: (value: SuspendAccessResponse): Buffer =>
      Buffer.from(SuspendAccessResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SuspendAccessResponse => SuspendAccessResponse.decode(value),
  },
  restoreAccess: {
    path: "/reducbox.ReducBoxService/RestoreAccess",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: RestoreAccessRequest): Buffer => Buffer.from(RestoreAccessRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RestoreAccessRequest => RestoreAccessRequest.decode(value),
    responseSerialize: (value: RestoreAccessResponse): Buffer =>
      Buffer.from(RestoreAccessResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RestoreAccessResponse => RestoreAccessResponse.decode(value),
  },
  getAccessStatus: {
    path: "/reducbox.ReducBoxService/GetAccessStatus",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GetAccessStatusRequest): Buffer =>
      Buffer.from(GetAccessStatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetAccessStatusRequest => GetAccessStatusRequest.decode(value),
    responseSerialize: (value: GetAccessStatusResponse): Buffer =>
      Buffer.from(GetAccessStatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetAccessStatusResponse => GetAccessStatusResponse.decode(value),
  },
  listAccessByClient: {
    path: "/reducbox.ReducBoxService/ListAccessByClient",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: ListAccessByClientRequest): Buffer =>
      Buffer.from(ListAccessByClientRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListAccessByClientRequest => ListAccessByClientRequest.decode(value),
    responseSerialize: (value: ListAccessByClientResponse): Buffer =>
      Buffer.from(ListAccessByClientResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListAccessByClientResponse => ListAccessByClientResponse.decode(value),
  },
} as const;

export interface ReducBoxServiceServer extends UntypedServiceImplementation {
  createAccess: handleUnaryCall<CreateAccessRequest, CreateAccessResponse>;
  suspendAccess: handleUnaryCall<SuspendAccessRequest, SuspendAccessResponse>;
  restoreAccess: handleUnaryCall<RestoreAccessRequest, RestoreAccessResponse>;
  getAccessStatus: handleUnaryCall<GetAccessStatusRequest, GetAccessStatusResponse>;
  listAccessByClient: handleUnaryCall<ListAccessByClientRequest, ListAccessByClientResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
