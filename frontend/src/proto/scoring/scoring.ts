// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: scoring/scoring.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, UntypedServiceImplementation } from "@grpc/grpc-js";

export const protobufPackage = "scoring";

export interface PredictRiskRequest {
  prevRejects: number;
  channel: string;
  contractAgeMonths: number;
  paymentHistoryCount: number;
  lotCode: string;
  provider: string;
  amountCents: number;
  preferredDebitDay: number;
}

export interface PredictRiskResponse {
  score: number;
  riskTier: string;
  factors: { [key: string]: number };
}

export interface PredictRiskResponse_FactorsEntry {
  key: string;
  value: number;
}

function createBasePredictRiskRequest(): PredictRiskRequest {
  return {
    prevRejects: 0,
    channel: "",
    contractAgeMonths: 0,
    paymentHistoryCount: 0,
    lotCode: "",
    provider: "",
    amountCents: 0,
    preferredDebitDay: 0,
  };
}

export const PredictRiskRequest: MessageFns<PredictRiskRequest> = {
  encode(message: PredictRiskRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prevRejects !== 0) {
      writer.uint32(8).int32(message.prevRejects);
    }
    if (message.channel !== "") {
      writer.uint32(18).string(message.channel);
    }
    if (message.contractAgeMonths !== 0) {
      writer.uint32(24).int32(message.contractAgeMonths);
    }
    if (message.paymentHistoryCount !== 0) {
      writer.uint32(32).int32(message.paymentHistoryCount);
    }
    if (message.lotCode !== "") {
      writer.uint32(42).string(message.lotCode);
    }
    if (message.provider !== "") {
      writer.uint32(50).string(message.provider);
    }
    if (message.amountCents !== 0) {
      writer.uint32(56).int32(message.amountCents);
    }
    if (message.preferredDebitDay !== 0) {
      writer.uint32(64).int32(message.preferredDebitDay);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PredictRiskRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePredictRiskRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.prevRejects = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.channel = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.contractAgeMonths = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.paymentHistoryCount = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.lotCode = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.amountCents = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.preferredDebitDay = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PredictRiskRequest {
    return {
      prevRejects: isSet(object.prevRejects)
        ? globalThis.Number(object.prevRejects)
        : isSet(object.prev_rejects)
        ? globalThis.Number(object.prev_rejects)
        : 0,
      channel: isSet(object.channel) ? globalThis.String(object.channel) : "",
      contractAgeMonths: isSet(object.contractAgeMonths)
        ? globalThis.Number(object.contractAgeMonths)
        : isSet(object.contract_age_months)
        ? globalThis.Number(object.contract_age_months)
        : 0,
      paymentHistoryCount: isSet(object.paymentHistoryCount)
        ? globalThis.Number(object.paymentHistoryCount)
        : isSet(object.payment_history_count)
        ? globalThis.Number(object.payment_history_count)
        : 0,
      lotCode: isSet(object.lotCode)
        ? globalThis.String(object.lotCode)
        : isSet(object.lot_code)
        ? globalThis.String(object.lot_code)
        : "",
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      amountCents: isSet(object.amountCents)
        ? globalThis.Number(object.amountCents)
        : isSet(object.amount_cents)
        ? globalThis.Number(object.amount_cents)
        : 0,
      preferredDebitDay: isSet(object.preferredDebitDay)
        ? globalThis.Number(object.preferredDebitDay)
        : isSet(object.preferred_debit_day)
        ? globalThis.Number(object.preferred_debit_day)
        : 0,
    };
  },

  toJSON(message: PredictRiskRequest): unknown {
    const obj: any = {};
    if (message.prevRejects !== 0) {
      obj.prevRejects = Math.round(message.prevRejects);
    }
    if (message.channel !== "") {
      obj.channel = message.channel;
    }
    if (message.contractAgeMonths !== 0) {
      obj.contractAgeMonths = Math.round(message.contractAgeMonths);
    }
    if (message.paymentHistoryCount !== 0) {
      obj.paymentHistoryCount = Math.round(message.paymentHistoryCount);
    }
    if (message.lotCode !== "") {
      obj.lotCode = message.lotCode;
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.amountCents !== 0) {
      obj.amountCents = Math.round(message.amountCents);
    }
    if (message.preferredDebitDay !== 0) {
      obj.preferredDebitDay = Math.round(message.preferredDebitDay);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PredictRiskRequest>, I>>(base?: I): PredictRiskRequest {
    return PredictRiskRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PredictRiskRequest>, I>>(object: I): PredictRiskRequest {
    const message = createBasePredictRiskRequest();
    message.prevRejects = object.prevRejects ?? 0;
    message.channel = object.channel ?? "";
    message.contractAgeMonths = object.contractAgeMonths ?? 0;
    message.paymentHistoryCount = object.paymentHistoryCount ?? 0;
    message.lotCode = object.lotCode ?? "";
    message.provider = object.provider ?? "";
    message.amountCents = object.amountCents ?? 0;
    message.preferredDebitDay = object.preferredDebitDay ?? 0;
    return message;
  },
};

function createBasePredictRiskResponse(): PredictRiskResponse {
  return { score: 0, riskTier: "", factors: {} };
}

export const PredictRiskResponse: MessageFns<PredictRiskResponse> = {
  encode(message: PredictRiskResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.score !== 0) {
      writer.uint32(8).int32(message.score);
    }
    if (message.riskTier !== "") {
      writer.uint32(18).string(message.riskTier);
    }
    globalThis.Object.entries(message.factors).forEach(([key, value]: [string, number]) => {
      PredictRiskResponse_FactorsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PredictRiskResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePredictRiskResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.score = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.riskTier = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = PredictRiskResponse_FactorsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.factors[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PredictRiskResponse {
    return {
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      riskTier: isSet(object.riskTier)
        ? globalThis.String(object.riskTier)
        : isSet(object.risk_tier)
        ? globalThis.String(object.risk_tier)
        : "",
      factors: isObject(object.factors)
        ? (globalThis.Object.entries(object.factors) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: PredictRiskResponse): unknown {
    const obj: any = {};
    if (message.score !== 0) {
      obj.score = Math.round(message.score);
    }
    if (message.riskTier !== "") {
      obj.riskTier = message.riskTier;
    }
    if (message.factors) {
      const entries = globalThis.Object.entries(message.factors) as [string, number][];
      if (entries.length > 0) {
        obj.factors = {};
        entries.forEach(([k, v]) => {
          obj.factors[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PredictRiskResponse>, I>>(base?: I): PredictRiskResponse {
    return PredictRiskResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PredictRiskResponse>, I>>(object: I): PredictRiskResponse {
    const message = createBasePredictRiskResponse();
    message.score = object.score ?? 0;
    message.riskTier = object.riskTier ?? "";
    message.factors = (globalThis.Object.entries(object.factors ?? {}) as [string, number][]).reduce(
      (acc: { [key: string]: number }, [key, value]: [string, number]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBasePredictRiskResponse_FactorsEntry(): PredictRiskResponse_FactorsEntry {
  return { key: "", value: 0 };
}

export const PredictRiskResponse_FactorsEntry: MessageFns<PredictRiskResponse_FactorsEntry> = {
  encode(message: PredictRiskResponse_FactorsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(17).double(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PredictRiskResponse_FactorsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePredictRiskResponse_FactorsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.value = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PredictRiskResponse_FactorsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: PredictRiskResponse_FactorsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PredictRiskResponse_FactorsEntry>, I>>(
    base?: I,
  ): PredictRiskResponse_FactorsEntry {
    return PredictRiskResponse_FactorsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PredictRiskResponse_FactorsEntry>, I>>(
    object: I,
  ): PredictRiskResponse_FactorsEntry {
    const message = createBasePredictRiskResponse_FactorsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

export type ScoringServiceService = typeof ScoringServiceService;
export const ScoringServiceService = {
  predictRisk: {
    path: "/scoring.ScoringService/PredictRisk",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: PredictRiskRequest): Buffer => Buffer.from(PredictRiskRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PredictRiskRequest => PredictRiskRequest.decode(value),
    responseSerialize: (value: PredictRiskResponse): Buffer => Buffer.from(PredictRiskResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PredictRiskResponse => PredictRiskResponse.decode(value),
  },
} as const;

export interface ScoringServiceServer extends UntypedServiceImplementation {
  predictRisk: handleUnaryCall<PredictRiskRequest, PredictRiskResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
