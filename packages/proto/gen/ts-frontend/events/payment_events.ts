// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: events/payment_events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";

/** Payment received event */
export interface PaymentReceivedEvent {
  /** Event metadata */
  eventId: string;
  /** Unix timestamp in milliseconds */
  timestamp: number;
  /** Optional, for tracing */
  correlationId: string;
  /** Event data */
  paymentId: string;
  /** Payment schedule ID */
  scheduleId: string;
  /** Client ID */
  clientId: string;
  /** Amount in EUR */
  montant: number;
  /** Reception date */
  dateReception?: Date | undefined;
}

/** Payment rejected event */
export interface PaymentRejectedEvent {
  /** Event metadata */
  eventId: string;
  /** Unix timestamp in milliseconds */
  timestamp: number;
  /** Optional, for tracing */
  correlationId: string;
  /** Event data */
  paymentId: string;
  /** Payment schedule ID */
  scheduleId: string;
  /** Client ID */
  clientId: string;
  /** Rejection reason (e.g., AM04) */
  motifRejet: string;
  /** Rejection date */
  dateRejet?: Date | undefined;
}

function createBasePaymentReceivedEvent(): PaymentReceivedEvent {
  return {
    eventId: "",
    timestamp: 0,
    correlationId: "",
    paymentId: "",
    scheduleId: "",
    clientId: "",
    montant: 0,
    dateReception: undefined,
  };
}

export const PaymentReceivedEvent: MessageFns<PaymentReceivedEvent> = {
  encode(message: PaymentReceivedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).int64(message.timestamp);
    }
    if (message.correlationId !== "") {
      writer.uint32(26).string(message.correlationId);
    }
    if (message.paymentId !== "") {
      writer.uint32(34).string(message.paymentId);
    }
    if (message.scheduleId !== "") {
      writer.uint32(42).string(message.scheduleId);
    }
    if (message.clientId !== "") {
      writer.uint32(50).string(message.clientId);
    }
    if (message.montant !== 0) {
      writer.uint32(57).double(message.montant);
    }
    if (message.dateReception !== undefined) {
      Timestamp.encode(toTimestamp(message.dateReception), writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PaymentReceivedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePaymentReceivedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.paymentId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.scheduleId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 57) {
            break;
          }

          message.montant = reader.double();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.dateReception = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PaymentReceivedEvent {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : "",
      paymentId: isSet(object.paymentId)
        ? globalThis.String(object.paymentId)
        : isSet(object.payment_id)
        ? globalThis.String(object.payment_id)
        : "",
      scheduleId: isSet(object.scheduleId)
        ? globalThis.String(object.scheduleId)
        : isSet(object.schedule_id)
        ? globalThis.String(object.schedule_id)
        : "",
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      montant: isSet(object.montant) ? globalThis.Number(object.montant) : 0,
      dateReception: isSet(object.dateReception)
        ? fromJsonTimestamp(object.dateReception)
        : isSet(object.date_reception)
        ? fromJsonTimestamp(object.date_reception)
        : undefined,
    };
  },

  toJSON(message: PaymentReceivedEvent): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.correlationId !== "") {
      obj.correlationId = message.correlationId;
    }
    if (message.paymentId !== "") {
      obj.paymentId = message.paymentId;
    }
    if (message.scheduleId !== "") {
      obj.scheduleId = message.scheduleId;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.montant !== 0) {
      obj.montant = message.montant;
    }
    if (message.dateReception !== undefined) {
      obj.dateReception = message.dateReception.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PaymentReceivedEvent>, I>>(base?: I): PaymentReceivedEvent {
    return PaymentReceivedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PaymentReceivedEvent>, I>>(object: I): PaymentReceivedEvent {
    const message = createBasePaymentReceivedEvent();
    message.eventId = object.eventId ?? "";
    message.timestamp = object.timestamp ?? 0;
    message.correlationId = object.correlationId ?? "";
    message.paymentId = object.paymentId ?? "";
    message.scheduleId = object.scheduleId ?? "";
    message.clientId = object.clientId ?? "";
    message.montant = object.montant ?? 0;
    message.dateReception = object.dateReception ?? undefined;
    return message;
  },
};

function createBasePaymentRejectedEvent(): PaymentRejectedEvent {
  return {
    eventId: "",
    timestamp: 0,
    correlationId: "",
    paymentId: "",
    scheduleId: "",
    clientId: "",
    motifRejet: "",
    dateRejet: undefined,
  };
}

export const PaymentRejectedEvent: MessageFns<PaymentRejectedEvent> = {
  encode(message: PaymentRejectedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).int64(message.timestamp);
    }
    if (message.correlationId !== "") {
      writer.uint32(26).string(message.correlationId);
    }
    if (message.paymentId !== "") {
      writer.uint32(34).string(message.paymentId);
    }
    if (message.scheduleId !== "") {
      writer.uint32(42).string(message.scheduleId);
    }
    if (message.clientId !== "") {
      writer.uint32(50).string(message.clientId);
    }
    if (message.motifRejet !== "") {
      writer.uint32(58).string(message.motifRejet);
    }
    if (message.dateRejet !== undefined) {
      Timestamp.encode(toTimestamp(message.dateRejet), writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PaymentRejectedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePaymentRejectedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.paymentId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.scheduleId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.motifRejet = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.dateRejet = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PaymentRejectedEvent {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : "",
      paymentId: isSet(object.paymentId)
        ? globalThis.String(object.paymentId)
        : isSet(object.payment_id)
        ? globalThis.String(object.payment_id)
        : "",
      scheduleId: isSet(object.scheduleId)
        ? globalThis.String(object.scheduleId)
        : isSet(object.schedule_id)
        ? globalThis.String(object.schedule_id)
        : "",
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      motifRejet: isSet(object.motifRejet)
        ? globalThis.String(object.motifRejet)
        : isSet(object.motif_rejet)
        ? globalThis.String(object.motif_rejet)
        : "",
      dateRejet: isSet(object.dateRejet)
        ? fromJsonTimestamp(object.dateRejet)
        : isSet(object.date_rejet)
        ? fromJsonTimestamp(object.date_rejet)
        : undefined,
    };
  },

  toJSON(message: PaymentRejectedEvent): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.correlationId !== "") {
      obj.correlationId = message.correlationId;
    }
    if (message.paymentId !== "") {
      obj.paymentId = message.paymentId;
    }
    if (message.scheduleId !== "") {
      obj.scheduleId = message.scheduleId;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.motifRejet !== "") {
      obj.motifRejet = message.motifRejet;
    }
    if (message.dateRejet !== undefined) {
      obj.dateRejet = message.dateRejet.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PaymentRejectedEvent>, I>>(base?: I): PaymentRejectedEvent {
    return PaymentRejectedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PaymentRejectedEvent>, I>>(object: I): PaymentRejectedEvent {
    const message = createBasePaymentRejectedEvent();
    message.eventId = object.eventId ?? "";
    message.timestamp = object.timestamp ?? 0;
    message.correlationId = object.correlationId ?? "";
    message.paymentId = object.paymentId ?? "";
    message.scheduleId = object.scheduleId ?? "";
    message.clientId = object.clientId ?? "";
    message.motifRejet = object.motifRejet ?? "";
    message.dateRejet = object.dateRejet ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
