// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: security/options.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

/** Data classification levels for sensitive data handling */
export enum DataClassification {
  DATA_CLASSIFICATION_UNSPECIFIED = 0,
  /** DATA_CLASSIFICATION_PUBLIC - Public data - no restrictions */
  DATA_CLASSIFICATION_PUBLIC = 1,
  /** DATA_CLASSIFICATION_INTERNAL - Internal data - no encryption required, basic audit */
  DATA_CLASSIFICATION_INTERNAL = 2,
  /** DATA_CLASSIFICATION_PII - Personal Identifiable Information - encryption required, GDPR applies */
  DATA_CLASSIFICATION_PII = 3,
  /** DATA_CLASSIFICATION_PAYMENT - Payment-related data - encryption + enhanced audit */
  DATA_CLASSIFICATION_PAYMENT = 4,
  /** DATA_CLASSIFICATION_SECRET - Secrets - never log, always encrypt, restricted access */
  DATA_CLASSIFICATION_SECRET = 5,
}

export function dataClassificationFromJSON(object: any): DataClassification {
  switch (object) {
    case 0:
    case "DATA_CLASSIFICATION_UNSPECIFIED":
      return DataClassification.DATA_CLASSIFICATION_UNSPECIFIED;
    case 1:
    case "DATA_CLASSIFICATION_PUBLIC":
      return DataClassification.DATA_CLASSIFICATION_PUBLIC;
    case 2:
    case "DATA_CLASSIFICATION_INTERNAL":
      return DataClassification.DATA_CLASSIFICATION_INTERNAL;
    case 3:
    case "DATA_CLASSIFICATION_PII":
      return DataClassification.DATA_CLASSIFICATION_PII;
    case 4:
    case "DATA_CLASSIFICATION_PAYMENT":
      return DataClassification.DATA_CLASSIFICATION_PAYMENT;
    case 5:
    case "DATA_CLASSIFICATION_SECRET":
      return DataClassification.DATA_CLASSIFICATION_SECRET;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum DataClassification");
  }
}

export function dataClassificationToJSON(object: DataClassification): string {
  switch (object) {
    case DataClassification.DATA_CLASSIFICATION_UNSPECIFIED:
      return "DATA_CLASSIFICATION_UNSPECIFIED";
    case DataClassification.DATA_CLASSIFICATION_PUBLIC:
      return "DATA_CLASSIFICATION_PUBLIC";
    case DataClassification.DATA_CLASSIFICATION_INTERNAL:
      return "DATA_CLASSIFICATION_INTERNAL";
    case DataClassification.DATA_CLASSIFICATION_PII:
      return "DATA_CLASSIFICATION_PII";
    case DataClassification.DATA_CLASSIFICATION_PAYMENT:
      return "DATA_CLASSIFICATION_PAYMENT";
    case DataClassification.DATA_CLASSIFICATION_SECRET:
      return "DATA_CLASSIFICATION_SECRET";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum DataClassification");
  }
}

/**
 * Permission requirement for a method
 * Used by security interceptor to enforce access control
 */
export interface PermissionRequirement {
  /**
   * Permission code required (e.g., "payment:refund:create")
   * Format: resource:action[:sub_resource]
   */
  permission: string;
  /**
   * If true, request must include organisation_id and actor must be member
   * Enforces tenant isolation
   */
  tenantScoped: boolean;
  /**
   * If true, MFA must have been verified within the step-up window
   * Window is configurable per permission (default: 10 minutes)
   */
  mfaStepUpRequired: boolean;
  /**
   * If true, audit write MUST succeed or the RPC fails
   * Critical for sensitive operations
   */
  auditBlocking: boolean;
  /**
   * Logical audit action label (e.g., "payment.refund.create")
   * Used in audit logs for consistent action naming
   */
  auditAction: string;
  /**
   * Data classification for this method
   * Affects logging, encryption, and retention policies
   */
  dataClassification: DataClassification;
  /**
   * Step-up window in seconds (default: 600 = 10 minutes)
   * Only applies when mfa_step_up_required = true
   */
  stepUpWindowSeconds: number;
  /** Description for documentation and error messages */
  description: string;
}

/** Service-level security configuration */
export interface ServiceSecurityConfig {
  /** Default tenant scoping for all methods (can be overridden per method) */
  defaultTenantScoped: boolean;
  /** Default data classification for all methods */
  defaultDataClassification: DataClassification;
  /** Service identity for mTLS (e.g., "payment-service") */
  serviceIdentity: string;
  /** List of services allowed to call this service (for mTLS allowlist) */
  allowedCallers: string[];
}

/** Field-level security annotations */
export interface FieldSecurityConfig {
  /** If true, this field must be encrypted at rest */
  encrypted: boolean;
  /** If true, generate a blind index for searchable encryption */
  searchable: boolean;
  /** If true, field should be redacted in logs */
  redactInLogs: boolean;
  /** Data classification for this field */
  classification: DataClassification;
  /** PII category (for GDPR tracking) */
  piiCategory: string;
}

/** Message-level security annotations */
export interface MessageSecurityConfig {
  /** If true, entire message should be treated as sensitive */
  sensitive: boolean;
  /** Data classification for this message type */
  classification: DataClassification;
  /** Retention category (for purge policies) */
  retentionCategory: string;
}

function createBasePermissionRequirement(): PermissionRequirement {
  return {
    permission: "",
    tenantScoped: false,
    mfaStepUpRequired: false,
    auditBlocking: false,
    auditAction: "",
    dataClassification: 0,
    stepUpWindowSeconds: 0,
    description: "",
  };
}

export const PermissionRequirement: MessageFns<PermissionRequirement> = {
  encode(message: PermissionRequirement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.permission !== "") {
      writer.uint32(10).string(message.permission);
    }
    if (message.tenantScoped !== false) {
      writer.uint32(16).bool(message.tenantScoped);
    }
    if (message.mfaStepUpRequired !== false) {
      writer.uint32(24).bool(message.mfaStepUpRequired);
    }
    if (message.auditBlocking !== false) {
      writer.uint32(32).bool(message.auditBlocking);
    }
    if (message.auditAction !== "") {
      writer.uint32(42).string(message.auditAction);
    }
    if (message.dataClassification !== 0) {
      writer.uint32(48).int32(message.dataClassification);
    }
    if (message.stepUpWindowSeconds !== 0) {
      writer.uint32(56).int32(message.stepUpWindowSeconds);
    }
    if (message.description !== "") {
      writer.uint32(66).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PermissionRequirement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePermissionRequirement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.permission = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tenantScoped = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.mfaStepUpRequired = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.auditBlocking = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.auditAction = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.dataClassification = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.stepUpWindowSeconds = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PermissionRequirement {
    return {
      permission: isSet(object.permission) ? globalThis.String(object.permission) : "",
      tenantScoped: isSet(object.tenantScoped)
        ? globalThis.Boolean(object.tenantScoped)
        : isSet(object.tenant_scoped)
        ? globalThis.Boolean(object.tenant_scoped)
        : false,
      mfaStepUpRequired: isSet(object.mfaStepUpRequired)
        ? globalThis.Boolean(object.mfaStepUpRequired)
        : isSet(object.mfa_step_up_required)
        ? globalThis.Boolean(object.mfa_step_up_required)
        : false,
      auditBlocking: isSet(object.auditBlocking)
        ? globalThis.Boolean(object.auditBlocking)
        : isSet(object.audit_blocking)
        ? globalThis.Boolean(object.audit_blocking)
        : false,
      auditAction: isSet(object.auditAction)
        ? globalThis.String(object.auditAction)
        : isSet(object.audit_action)
        ? globalThis.String(object.audit_action)
        : "",
      dataClassification: isSet(object.dataClassification)
        ? dataClassificationFromJSON(object.dataClassification)
        : isSet(object.data_classification)
        ? dataClassificationFromJSON(object.data_classification)
        : 0,
      stepUpWindowSeconds: isSet(object.stepUpWindowSeconds)
        ? globalThis.Number(object.stepUpWindowSeconds)
        : isSet(object.step_up_window_seconds)
        ? globalThis.Number(object.step_up_window_seconds)
        : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: PermissionRequirement): unknown {
    const obj: any = {};
    if (message.permission !== "") {
      obj.permission = message.permission;
    }
    if (message.tenantScoped !== false) {
      obj.tenantScoped = message.tenantScoped;
    }
    if (message.mfaStepUpRequired !== false) {
      obj.mfaStepUpRequired = message.mfaStepUpRequired;
    }
    if (message.auditBlocking !== false) {
      obj.auditBlocking = message.auditBlocking;
    }
    if (message.auditAction !== "") {
      obj.auditAction = message.auditAction;
    }
    if (message.dataClassification !== 0) {
      obj.dataClassification = dataClassificationToJSON(message.dataClassification);
    }
    if (message.stepUpWindowSeconds !== 0) {
      obj.stepUpWindowSeconds = Math.round(message.stepUpWindowSeconds);
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PermissionRequirement>, I>>(base?: I): PermissionRequirement {
    return PermissionRequirement.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PermissionRequirement>, I>>(object: I): PermissionRequirement {
    const message = createBasePermissionRequirement();
    message.permission = object.permission ?? "";
    message.tenantScoped = object.tenantScoped ?? false;
    message.mfaStepUpRequired = object.mfaStepUpRequired ?? false;
    message.auditBlocking = object.auditBlocking ?? false;
    message.auditAction = object.auditAction ?? "";
    message.dataClassification = object.dataClassification ?? 0;
    message.stepUpWindowSeconds = object.stepUpWindowSeconds ?? 0;
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseServiceSecurityConfig(): ServiceSecurityConfig {
  return { defaultTenantScoped: false, defaultDataClassification: 0, serviceIdentity: "", allowedCallers: [] };
}

export const ServiceSecurityConfig: MessageFns<ServiceSecurityConfig> = {
  encode(message: ServiceSecurityConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.defaultTenantScoped !== false) {
      writer.uint32(8).bool(message.defaultTenantScoped);
    }
    if (message.defaultDataClassification !== 0) {
      writer.uint32(16).int32(message.defaultDataClassification);
    }
    if (message.serviceIdentity !== "") {
      writer.uint32(26).string(message.serviceIdentity);
    }
    for (const v of message.allowedCallers) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceSecurityConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceSecurityConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.defaultTenantScoped = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.defaultDataClassification = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.serviceIdentity = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.allowedCallers.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceSecurityConfig {
    return {
      defaultTenantScoped: isSet(object.defaultTenantScoped)
        ? globalThis.Boolean(object.defaultTenantScoped)
        : isSet(object.default_tenant_scoped)
        ? globalThis.Boolean(object.default_tenant_scoped)
        : false,
      defaultDataClassification: isSet(object.defaultDataClassification)
        ? dataClassificationFromJSON(object.defaultDataClassification)
        : isSet(object.default_data_classification)
        ? dataClassificationFromJSON(object.default_data_classification)
        : 0,
      serviceIdentity: isSet(object.serviceIdentity)
        ? globalThis.String(object.serviceIdentity)
        : isSet(object.service_identity)
        ? globalThis.String(object.service_identity)
        : "",
      allowedCallers: globalThis.Array.isArray(object?.allowedCallers)
        ? object.allowedCallers.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.allowed_callers)
        ? object.allowed_callers.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ServiceSecurityConfig): unknown {
    const obj: any = {};
    if (message.defaultTenantScoped !== false) {
      obj.defaultTenantScoped = message.defaultTenantScoped;
    }
    if (message.defaultDataClassification !== 0) {
      obj.defaultDataClassification = dataClassificationToJSON(message.defaultDataClassification);
    }
    if (message.serviceIdentity !== "") {
      obj.serviceIdentity = message.serviceIdentity;
    }
    if (message.allowedCallers?.length) {
      obj.allowedCallers = message.allowedCallers;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceSecurityConfig>, I>>(base?: I): ServiceSecurityConfig {
    return ServiceSecurityConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceSecurityConfig>, I>>(object: I): ServiceSecurityConfig {
    const message = createBaseServiceSecurityConfig();
    message.defaultTenantScoped = object.defaultTenantScoped ?? false;
    message.defaultDataClassification = object.defaultDataClassification ?? 0;
    message.serviceIdentity = object.serviceIdentity ?? "";
    message.allowedCallers = object.allowedCallers?.map((e) => e) || [];
    return message;
  },
};

function createBaseFieldSecurityConfig(): FieldSecurityConfig {
  return { encrypted: false, searchable: false, redactInLogs: false, classification: 0, piiCategory: "" };
}

export const FieldSecurityConfig: MessageFns<FieldSecurityConfig> = {
  encode(message: FieldSecurityConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.encrypted !== false) {
      writer.uint32(8).bool(message.encrypted);
    }
    if (message.searchable !== false) {
      writer.uint32(16).bool(message.searchable);
    }
    if (message.redactInLogs !== false) {
      writer.uint32(24).bool(message.redactInLogs);
    }
    if (message.classification !== 0) {
      writer.uint32(32).int32(message.classification);
    }
    if (message.piiCategory !== "") {
      writer.uint32(42).string(message.piiCategory);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FieldSecurityConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFieldSecurityConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.encrypted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.searchable = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.redactInLogs = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.classification = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.piiCategory = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FieldSecurityConfig {
    return {
      encrypted: isSet(object.encrypted) ? globalThis.Boolean(object.encrypted) : false,
      searchable: isSet(object.searchable) ? globalThis.Boolean(object.searchable) : false,
      redactInLogs: isSet(object.redactInLogs)
        ? globalThis.Boolean(object.redactInLogs)
        : isSet(object.redact_in_logs)
        ? globalThis.Boolean(object.redact_in_logs)
        : false,
      classification: isSet(object.classification) ? dataClassificationFromJSON(object.classification) : 0,
      piiCategory: isSet(object.piiCategory)
        ? globalThis.String(object.piiCategory)
        : isSet(object.pii_category)
        ? globalThis.String(object.pii_category)
        : "",
    };
  },

  toJSON(message: FieldSecurityConfig): unknown {
    const obj: any = {};
    if (message.encrypted !== false) {
      obj.encrypted = message.encrypted;
    }
    if (message.searchable !== false) {
      obj.searchable = message.searchable;
    }
    if (message.redactInLogs !== false) {
      obj.redactInLogs = message.redactInLogs;
    }
    if (message.classification !== 0) {
      obj.classification = dataClassificationToJSON(message.classification);
    }
    if (message.piiCategory !== "") {
      obj.piiCategory = message.piiCategory;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FieldSecurityConfig>, I>>(base?: I): FieldSecurityConfig {
    return FieldSecurityConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FieldSecurityConfig>, I>>(object: I): FieldSecurityConfig {
    const message = createBaseFieldSecurityConfig();
    message.encrypted = object.encrypted ?? false;
    message.searchable = object.searchable ?? false;
    message.redactInLogs = object.redactInLogs ?? false;
    message.classification = object.classification ?? 0;
    message.piiCategory = object.piiCategory ?? "";
    return message;
  },
};

function createBaseMessageSecurityConfig(): MessageSecurityConfig {
  return { sensitive: false, classification: 0, retentionCategory: "" };
}

export const MessageSecurityConfig: MessageFns<MessageSecurityConfig> = {
  encode(message: MessageSecurityConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sensitive !== false) {
      writer.uint32(8).bool(message.sensitive);
    }
    if (message.classification !== 0) {
      writer.uint32(16).int32(message.classification);
    }
    if (message.retentionCategory !== "") {
      writer.uint32(26).string(message.retentionCategory);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageSecurityConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageSecurityConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sensitive = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.classification = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.retentionCategory = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageSecurityConfig {
    return {
      sensitive: isSet(object.sensitive) ? globalThis.Boolean(object.sensitive) : false,
      classification: isSet(object.classification) ? dataClassificationFromJSON(object.classification) : 0,
      retentionCategory: isSet(object.retentionCategory)
        ? globalThis.String(object.retentionCategory)
        : isSet(object.retention_category)
        ? globalThis.String(object.retention_category)
        : "",
    };
  },

  toJSON(message: MessageSecurityConfig): unknown {
    const obj: any = {};
    if (message.sensitive !== false) {
      obj.sensitive = message.sensitive;
    }
    if (message.classification !== 0) {
      obj.classification = dataClassificationToJSON(message.classification);
    }
    if (message.retentionCategory !== "") {
      obj.retentionCategory = message.retentionCategory;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessageSecurityConfig>, I>>(base?: I): MessageSecurityConfig {
    return MessageSecurityConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessageSecurityConfig>, I>>(object: I): MessageSecurityConfig {
    const message = createBaseMessageSecurityConfig();
    message.sensitive = object.sensitive ?? false;
    message.classification = object.classification ?? 0;
    message.retentionCategory = object.retentionCategory ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
