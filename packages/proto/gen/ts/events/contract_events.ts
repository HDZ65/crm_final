// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: events/contract_events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";

/** Contract signed event */
export interface ContractSignedEvent {
  /** Event metadata */
  eventId: string;
  /** Unix timestamp in milliseconds */
  timestamp: number;
  /** Optional, for tracing */
  correlationId: string;
  /** Event data */
  contratId: string;
  /** Client ID */
  clientId: string;
  /** Product ID */
  produitId: string;
  /** Total amount in EUR */
  montantTotal: number;
  /** Signature date */
  dateSignature?: Timestamp | undefined;
}

function createBaseContractSignedEvent(): ContractSignedEvent {
  return { eventId: "", timestamp: 0, correlationId: "", contratId: "", clientId: "", produitId: "", montantTotal: 0 };
}

export const ContractSignedEvent: MessageFns<ContractSignedEvent> = {
  encode(message: ContractSignedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).int64(message.timestamp);
    }
    if (message.correlationId !== "") {
      writer.uint32(26).string(message.correlationId);
    }
    if (message.contratId !== "") {
      writer.uint32(34).string(message.contratId);
    }
    if (message.clientId !== "") {
      writer.uint32(42).string(message.clientId);
    }
    if (message.produitId !== "") {
      writer.uint32(50).string(message.produitId);
    }
    if (message.montantTotal !== 0) {
      writer.uint32(57).double(message.montantTotal);
    }
    if (message.dateSignature !== undefined) {
      Timestamp.encode(message.dateSignature, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContractSignedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContractSignedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.contratId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.produitId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 57) {
            break;
          }

          message.montantTotal = reader.double();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.dateSignature = Timestamp.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
