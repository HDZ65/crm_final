// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: security/options.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

/** Data classification levels for sensitive data handling */
export enum DataClassification {
  DATA_CLASSIFICATION_UNSPECIFIED = 0,
  /** DATA_CLASSIFICATION_PUBLIC - Public data - no restrictions */
  DATA_CLASSIFICATION_PUBLIC = 1,
  /** DATA_CLASSIFICATION_INTERNAL - Internal data - no encryption required, basic audit */
  DATA_CLASSIFICATION_INTERNAL = 2,
  /** DATA_CLASSIFICATION_PII - Personal Identifiable Information - encryption required, GDPR applies */
  DATA_CLASSIFICATION_PII = 3,
  /** DATA_CLASSIFICATION_PAYMENT - Payment-related data - encryption + enhanced audit */
  DATA_CLASSIFICATION_PAYMENT = 4,
  /** DATA_CLASSIFICATION_SECRET - Secrets - never log, always encrypt, restricted access */
  DATA_CLASSIFICATION_SECRET = 5,
}

/**
 * Permission requirement for a method
 * Used by security interceptor to enforce access control
 */
export interface PermissionRequirement {
  /**
   * Permission code required (e.g., "payment:refund:create")
   * Format: resource:action[:sub_resource]
   */
  permission: string;
  /**
   * If true, request must include organisation_id and actor must be member
   * Enforces tenant isolation
   */
  tenantScoped: boolean;
  /**
   * If true, MFA must have been verified within the step-up window
   * Window is configurable per permission (default: 10 minutes)
   */
  mfaStepUpRequired: boolean;
  /**
   * If true, audit write MUST succeed or the RPC fails
   * Critical for sensitive operations
   */
  auditBlocking: boolean;
  /**
   * Logical audit action label (e.g., "payment.refund.create")
   * Used in audit logs for consistent action naming
   */
  auditAction: string;
  /**
   * Data classification for this method
   * Affects logging, encryption, and retention policies
   */
  dataClassification: DataClassification;
  /**
   * Step-up window in seconds (default: 600 = 10 minutes)
   * Only applies when mfa_step_up_required = true
   */
  stepUpWindowSeconds: number;
  /** Description for documentation and error messages */
  description: string;
}

/** Service-level security configuration */
export interface ServiceSecurityConfig {
  /** Default tenant scoping for all methods (can be overridden per method) */
  defaultTenantScoped: boolean;
  /** Default data classification for all methods */
  defaultDataClassification: DataClassification;
  /** Service identity for mTLS (e.g., "payment-service") */
  serviceIdentity: string;
  /** List of services allowed to call this service (for mTLS allowlist) */
  allowedCallers: string[];
}

/** Field-level security annotations */
export interface FieldSecurityConfig {
  /** If true, this field must be encrypted at rest */
  encrypted: boolean;
  /** If true, generate a blind index for searchable encryption */
  searchable: boolean;
  /** If true, field should be redacted in logs */
  redactInLogs: boolean;
  /** Data classification for this field */
  classification: DataClassification;
  /** PII category (for GDPR tracking) */
  piiCategory: string;
}

/** Message-level security annotations */
export interface MessageSecurityConfig {
  /** If true, entire message should be treated as sensitive */
  sensitive: boolean;
  /** Data classification for this message type */
  classification: DataClassification;
  /** Retention category (for purge policies) */
  retentionCategory: string;
}

function createBasePermissionRequirement(): PermissionRequirement {
  return {
    permission: "",
    tenantScoped: false,
    mfaStepUpRequired: false,
    auditBlocking: false,
    auditAction: "",
    dataClassification: 0,
    stepUpWindowSeconds: 0,
    description: "",
  };
}

export const PermissionRequirement: MessageFns<PermissionRequirement> = {
  encode(message: PermissionRequirement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.permission !== "") {
      writer.uint32(10).string(message.permission);
    }
    if (message.tenantScoped !== false) {
      writer.uint32(16).bool(message.tenantScoped);
    }
    if (message.mfaStepUpRequired !== false) {
      writer.uint32(24).bool(message.mfaStepUpRequired);
    }
    if (message.auditBlocking !== false) {
      writer.uint32(32).bool(message.auditBlocking);
    }
    if (message.auditAction !== "") {
      writer.uint32(42).string(message.auditAction);
    }
    if (message.dataClassification !== 0) {
      writer.uint32(48).int32(message.dataClassification);
    }
    if (message.stepUpWindowSeconds !== 0) {
      writer.uint32(56).int32(message.stepUpWindowSeconds);
    }
    if (message.description !== "") {
      writer.uint32(66).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PermissionRequirement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePermissionRequirement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.permission = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tenantScoped = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.mfaStepUpRequired = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.auditBlocking = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.auditAction = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.dataClassification = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.stepUpWindowSeconds = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseServiceSecurityConfig(): ServiceSecurityConfig {
  return { defaultTenantScoped: false, defaultDataClassification: 0, serviceIdentity: "", allowedCallers: [] };
}

export const ServiceSecurityConfig: MessageFns<ServiceSecurityConfig> = {
  encode(message: ServiceSecurityConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.defaultTenantScoped !== false) {
      writer.uint32(8).bool(message.defaultTenantScoped);
    }
    if (message.defaultDataClassification !== 0) {
      writer.uint32(16).int32(message.defaultDataClassification);
    }
    if (message.serviceIdentity !== "") {
      writer.uint32(26).string(message.serviceIdentity);
    }
    for (const v of message.allowedCallers) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceSecurityConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceSecurityConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.defaultTenantScoped = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.defaultDataClassification = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.serviceIdentity = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.allowedCallers.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseFieldSecurityConfig(): FieldSecurityConfig {
  return { encrypted: false, searchable: false, redactInLogs: false, classification: 0, piiCategory: "" };
}

export const FieldSecurityConfig: MessageFns<FieldSecurityConfig> = {
  encode(message: FieldSecurityConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.encrypted !== false) {
      writer.uint32(8).bool(message.encrypted);
    }
    if (message.searchable !== false) {
      writer.uint32(16).bool(message.searchable);
    }
    if (message.redactInLogs !== false) {
      writer.uint32(24).bool(message.redactInLogs);
    }
    if (message.classification !== 0) {
      writer.uint32(32).int32(message.classification);
    }
    if (message.piiCategory !== "") {
      writer.uint32(42).string(message.piiCategory);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FieldSecurityConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFieldSecurityConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.encrypted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.searchable = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.redactInLogs = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.classification = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.piiCategory = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseMessageSecurityConfig(): MessageSecurityConfig {
  return { sensitive: false, classification: 0, retentionCategory: "" };
}

export const MessageSecurityConfig: MessageFns<MessageSecurityConfig> = {
  encode(message: MessageSecurityConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sensitive !== false) {
      writer.uint32(8).bool(message.sensitive);
    }
    if (message.classification !== 0) {
      writer.uint32(16).int32(message.classification);
    }
    if (message.retentionCategory !== "") {
      writer.uint32(26).string(message.retentionCategory);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageSecurityConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageSecurityConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sensitive = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.classification = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.retentionCategory = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
